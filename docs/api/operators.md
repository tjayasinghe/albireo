# Operators

Sparse linear operators — Doppler shift, LSF convolution (stationary, wavelength-dependent,
and asymmetric), interpolation, and flux-conserving rebinning — each with an exact adjoint
and an inner-product test asserting it.

::: albireo.operators
