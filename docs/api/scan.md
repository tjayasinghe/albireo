# Companion scan

The SB1 faint-companion detection scan: profile the marginal likelihood over the secondary
velocity semi-amplitude to search for a companion that never shows a second set of lines.

::: albireo.scan
