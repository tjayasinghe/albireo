# albireo — design document (v1)

**Status:** M0 draft for review. Companion document: [math.md](math.md) (all equations live
there; this document is about decisions).

albireo is an open-source Python package for **spectral disentangling** of double- and
multiple-lined spectroscopic binaries: given a time series of composite spectra, jointly infer
the orbit and the individual component spectra, with honest uncertainties on both. The name is
the mission: Albireo is the famous gold-and-blue double in Cygnus — albireo separates the gold
from the blue. Target users: massive-star binary surveys, dormant black-hole/neutron-star
companion searches, benchmark eclipsing binaries, and multi-epoch survey spectroscopy
(APOGEE/SDSS-V, WEAVE, 4MOST). Endgame: community-standard replacement for fd3/KOREL, a
methods paper (A&A/ApJS) + JOSS paper, pip-installable with tutorials from day one.

---

## 1. Prior art

| Method / code | Domain & approach | Strengths | Failure modes | We adopt / avoid |
|---|---|---|---|---|
| **Simon & Sturm 1994** (A&A 281, 286) | wavelength space; big sparse linear system, SVD, wrapped in orbit optimization | conceptually clean: disentangling *is* a linear inverse problem; natural weights/masks; arbitrary per-epoch RVs | SVD cost/memory blow up with N; ill-conditioned low-frequency nullspace, no regularization; no UQ | **Adopt** the linear-algebra formulation as our conceptual ancestor; replace SVD with structured/banded solvers + priors |
| **KOREL** (Hadrava 1995, A&AS 114, 393; 2004 guide; VO service, Škoda+2012) | Fourier space; simultaneous orbit + spectra; line-strength variability | most feature-rich incumbent; decades of use | Fourier low-frequency degeneracy; closed-source FORTRAN behind a registration-gated VO service (KOREL11b, 2011) — not scriptable or reproducible offline; formal errors long criticized; no UQ on spectra | **Avoid** Fourier space entirely; explain undulations as nullspace (math.md §5.1) |
| **fd3 / FDBinary** (Ilijić+2004, ASPC 318, 111; ascl:1705.012) | Fourier space, C + GSL, CLI | fast, template-free, triple-capable (hierarchical), scriptable, heavily cited | requires equidistant common log-λ sampling; Keplerian-only RVs; low-frequency "bias progression" (Hensberge+2008); no UQ anywhere; page last updated 2014, no license statement | **Avoid**; fd3 is the explicit benchmark target for accuracy + wall-time (M5) |
| **CRES** (Ilijić) / **Spectangular** (Sablowski & Weber 2017, A&A 597, A125; 2019, A&A 623, A31; C++/Qt, Apache-2.0) | wavelength space; SVD + global optimization of orbit or per-epoch RVs | wavelength-space flexibility (weights/masks); global optimizer; handles line-strength variability | CRES abandoned (2006), takes RVs as input; Spectangular GUI-centric, hard to batch, tiny community, point estimates only | direction validated; we add scalability + Bayesian inference |
| **Shift-and-add grid disentangling** (González & Levato 2006; Shenar et al. 2020, A&A 639, L6 & 641, A162 — LB-1/HR 6819; code: TomerShenar/Disentangling_Shift_And_Add) | iterative shift-and-add over a (K₁,K₂) χ² grid | simple, robust at low SNR, de facto standard in the massive-star/BH-candidate community; χ² maps show the K degeneracies | grid search scales exponentially in dimension; slow/stalling iteration = implicit regularizer nobody controls; **light ratios set by hand** (the crux of the LB-1/HR 6819 debate); no posterior; repo has **no license** (cannot vendor) | **Adopt** the use cases and diagnostics (K₂ scans, detection maps) as first-class features; replace the machinery |
| **PSOAP** (Czekala et al. 2017, ApJ 840, 49; MIT, dormant since 2017-12) | GP joint inference in wavelength space; component spectra marginalized analytically | principled joint Bayesian posterior over orbit *and* spectra — closest ancestor of albireo's design | **dense** GP covariances over epochs×pixels → cubic cost, quadratic memory → forced tiny chunks; NumPy/Cython, no autodiff/GPU; unmaintained | **Adopt** the philosophy; **avoid** dense kernels — banded *precision* priors + structured solves (math.md §2, §4) |
| **Seeburger et al. 2024** (MNRAS 530, 1935 — "autonomous disentangling for surveys") | wavelength space; second-derivative Tikhonov + sparse LSMR solve; Nelder–Mead over RVs/q | closest methodological competitor; survey-oriented; validated on the Unicorn/Giraffe | explicitly not Bayesian, no posteriors, no GPU/autodiff; **no public code** as of 2026-08 | their curvature penalty is exactly the deterministic limit of our τ·D₂ᵀD₂ prior — we get it for free *plus* the posterior; benchmark against it |
| **DOLBY** (Sairam et al. 2024, BEBOP VI, MNRAS) | sum of two Doppler-shifted GPs (wavelength- or CCF-space) | the one live GP/Bayesian effort since PSOAP | aimed at RV precision for circumbinary planets, not spectrum recovery; code availability unverified | adjacent niche; cite and compare |
| **BiSpeD** (González, Martínez & Alejo 2024, A&A 690, A124; Martínez & González 2025, A&A 694, A32; MIT, actively maintained) | SB1: grid over q, subtract primary, cross-correlate residuals vs. templates | owns the low-q (≲0.5) faint-companion regime; pip-friendly | not a general SB2 joint solver; template-grid dependent; grid search, no posterior | overlapping niche covered by our SB1 K₂-scan mode (template-free, marginalized) |

Also on the radar: QER20 (Quintero & Eenens 2024, MNRAS 532, 2604; code unverified) and
single-exposure NN parameter recovery (Binnenfeld et al. 2025) which *bypasses* disentangling.
A targeted search (2026-08) found **no JAX/GPU/differentiable/Bayesian disentangling code** —
the niche is unoccupied.

**The gap all of these leave:** no code offers (a) joint Bayesian orbit + spectra inference
with uncertainties on *both* — the field-wide workaround is bootstrap-over-epochs for orbit
errors and *nothing* for spectra — (b) at survey scale, (c) with modern engineering
(GPU, autodiff, tests, docs, pip). That triple is the product, and (a) is the sharpest edge.

**The differentiator** (math.md §3): conditional on the nonlinear parameters θ (orbit, light
fractions, LSF, response, hypers), the component spectra enter *linearly*. With Gaussian noise
and Gaussian smoothness priors they marginalize **analytically** → NUTS samples only ~10–200
nonlinear parameters regardless of spectrum size; spectra + full covariance are recovered
afterwards as a conditional Gaussian. This is the celerite/starry linear-marginalization trick
applied to disentangling; it is what makes the package categorically better than iterative
shift-and-add rather than incrementally better.

---

## 2. Decisions & recorded defaults

No blocking questions were open after the project brief; the following defaults were assumed
and are the ledger for review. Each is cheap to change now and expensive later.

| # | Decision | Default | Rationale / alternative |
|---|---|---|---|
| D1 | Model grid | uniform in ln λ; pixel `dv` = ~half the finest instrument pixel | shift = translation; oversampling controls interp error |
| D2 | Doppler mapping | relativistic, ξ(v) = artanh(v/c) | exactly antisymmetric ⇒ exact composition/inversion; classical off by ~0.6 km/s at 600 km/s; classical available as option |
| D3 | Shift interpolation | linear (2-tap), zero-fill, on *deviation* spectra d = s − 1 | sparse, exact adjoint; cubic 4-tap behind a flag later (math.md §1.1) |
| D4 | Data resampling | **never resample data**; model is projected to each epoch's native grid (pixel-integral rebin operator) | keeps noise diagonal; mixed-instrument for free |
| D5 | Frames | data frame declared per dataset (`topocentric` default); barycentric correction composed inside the model; tellurics = static component in topocentric frame | one convention, one function, tested |
| D6 | Tellurics | optional additive linear component (first-order approx of multiplicative transmission) | keeps linearity; error ~d★·d_tell, deep tellurics masked anyway; exact treatment is v2 |
| D7 | Response | multiplicative per-epoch Chebyshev, order 2 default, coefficients in θ (build-time constants through M4 — they enter the weights and targets, not just the operator, so their θ-swap was deferred; **built in D33**, math.md §7.5) | absorbs normalization errors; low order to protect broad features |
| D8 | LSF | per-instrument Gaussian σ_v (constant-R); tabulated LSF is v2 (banded matrix, no structural change; **built in D37** — per-anchor widths interpolated into a per-pixel profile bank, `operators.convolve_varying`, arbitrary asymmetric banks accepted by the operator) | mixed-resolution datasets supported from day one |
| D9 | Spectra priors | banded-precision smoothness (τ D₂ᵀD₂ + η I) per component; τ, η fixed/ML-II/sampled | scalable (no dense kernels); nullspace made proper *explicitly* |
| D10 | Marginal-likelihood engine | Strategy A: block-tridiagonal (banded) Cholesky, `lax.scan` + `vmap` over chunks/systems | ~10¹¹ flops at design target ⇒ ~0.1 s/eval on GPU; B (CG+SLQ) for benchmarks; C (frozen logdet) quick-look only (math.md §4) |
| D11 | Sampler | numpyro NUTS via `numpyro.factor` for the marginal likelihood (keeps priors/transforms/summaries); blackjax kept compatible | mature, well-documented, custom log-density path confirmed in numpyro docs |
| D12 | MAP | optax/jaxopt on the same objective; `fit_map()` is the quick-look path | |
| D13 | Light ratio | **no default** — API requires explicit `Fixed` / `Free(prior)` / `PerEpoch` | exact degeneracy with line depths (math.md §5.2); silence would be dishonest |
| D14 | Systemic velocity | γ ≡ 0 internally (spectra in systemic frame); free-γ only with informative prior | γ is unidentified by disentangling (math.md §5.3) |
| D15 | Noise model | diagonal ivar, mask = zero weight, optional per-epoch jitter factor (built in D31) | |
| D16 | Precision | float64 mandatory; enabled at `import albireo` (opt-out env var `ALBIREO_DISABLE_X64` for experiments) | adjoint/logdet tests need it; spectra work needs it |
| D17 | Stack | Python **≥3.12** (deviation from the brief's ≥3.11: current jaxlib 0.11.x ships no cp311 wheels), JAX, numpyro, optax; hatchling; src layout; ruff; pytest; GH Actions (ubuntu+windows, 3.12/3.13); mkdocs-material; BSD-3 | verified against PyPI 2026-08-11 |
| D18 | Package name | `albireo` — **verified free on PyPI** (2026-08-11); register early | fallback `albireo-spectra` if upload is admin-blocked |
| D19 | Versioning | SemVer, `0.1.0.dev0` now; public API = documented API | |
| D20 | Orbit parameterization | sampled sites (M3): `period`, `t_conj`, `secosw = √e cos ω`, `sesinw = √e sin ω`, `k` (vector); user-chosen numpyro priors supply the scale transforms (e.g. LogNormal for P, K); disk constraint e < 1 via −∞ factor with e clipped at 0.95 (Kepler solver's verified range); γ ≡ 0 (D14) | √e-pair smooth through e = 0 and uniform-disk ⇒ e ~ U(0,1); t_conj well-defined for circular orbits (math.md §7.2) |
| D21 | Static solver bandwidth | user declares `v_rel_max_kms`; bandwidth bound fixed at build time (jit-static); numpyro model adds a **bandwidth guard** (−∞ when realized shifts exceed the budget) | probing with too small a band is *silently* wrong — the guard makes wide priors safe by construction (math.md §7.1) |
| D22 | Spectral hyperparameters (τ, η) | empirical Bayes default: MAP jointly over (θ, log τ, log η) = ML-II (spectra already marginalized); NUTS runs with hypers conditioned at ML-II values; full sampling available by putting them in the priors dict | the prior scale *is* the answer for sub-LSF modes (math.md §5.1, §7.3) — it must be estimated deliberately, and the marginal's log det Λp Occam term makes ML-II well-posed |
| D23 | SB3 = hierarchical nested Keplerians | optional θ sites `period_out`, `t_conj_out`, `secosw_out`, `sesinw_out`, `k_out = (K_AB, K_C)` (all five or none); inner components ride the outer center-of-mass RV, tertiary opposes with ω_out + π; outer light-time effect neglected (v2 seam) | matches fd3's triple topology; the tertiary is just one more linear component (math.md §7.5) |
| D24 | Light fractions in θ | optional `light` site: `(n_stellar,)` constant or `(n_epochs, n_stellar)` per-epoch, rows on the simplex (Dirichlet priors); swaps into the static graph like the shifts | ℓ enters A(θ) linearly; the D13 eclipse breaker becomes *inferred*, closing the loop on math.md §5.2 |
| D25 | LSF widths in θ | optional `lsf_sigma` site (one per instrument, model-group order); construction-time widths fix the kernel radii and act as strict upper bounds (−∞ guard above); **absolute widths require a reference-instrument anchor** (tight prior) — only relative widths are data-identified | template-free models cannot split LSF from intrinsic line width (measured: all-free ML-II inflates widths ~tens of %, orbit unaffected); same honest-anchor policy as D13 |
| D26 | K₂-scan mode | `scan.k2_scan`: null = single-component model (ℓ₁ = 1, same primary prior); companion ℓ₂ explicit (D13); D(K₂) calibrated by injections, no χ² claim — Occam term makes D < 0 under the null (verified) | math.md §6; the marginalized matched filter is the BiSpeD/shift-and-add replacement |
| D27 | Scale architecture (M5) | `Problem` is a registered pytree passed as a jit *argument* (closure-captured arrays trigger multi-GB XLA constant folding at scale); probing runs as a **sequential scan** over probe batches with per-block gather readout — unrolled batches let XLA overlap live ranges (~80 GB temp at 2×10⁵ px, measured) and un-remat scans store every batch for the backward pass (~470 GB); batch size and backward rematerialization are **size-adaptive** (large batch / no remat below a ~64 MB probe-output footprint — remat measurably doubled gate-scale NUTS for zero benefit — small batch / remat above it); the prior factor gets its own small block size | benchmarks.md M5; `probe_chunk`/`remat` stay exposed as the CPU/GPU tuning knobs |
| D28 | Direct band assembly + closed-form gradient (M5 speedup pass) | the posterior precision is assembled **per epoch** from its analytic band structure — each epoch's (i,j) block is ℓᵢℓⱼ·T(δᵢ)ᵀ·(KᵀRᵀW′RK)·T(δⱼ), a band of width ~2·(rebin support + 2·kernel radius) at a velocity-set offset — via static rebin *pair tables* (`operators.rebin_pair_tables`, stored on `EpochGroup`), a vmapped kernel-sandwich pre-pass, and a `lax.scan` accumulating tent-weighted `dynamic_slice` translations into a band tensor (`assembly.band_block_tridiagonal`; math.md §4.5). Replaces 2p+1 global probe matvecs with O(band width) work per epoch (~8× eval measured); comb probing is retained as the reference path (`assembly="probe"`) and as the `validate=True` oracle. The solve stage carries a **custom VJP** (`likelihood._solve_stage`): cotangents from the block-Takahashi banded selected inverse (`solver.selected_inverse_blocks`) and d̂-outer-products, removing reverse-mode through the Cholesky/solve scans; gradients flow through `log_likelihood`, `quad`, and `d_hat` but deliberately **not** through the returned Cholesky factor (documented contract). Row translations use clip-safe `dynamic_slice` on double-padded arrays — exact zero-fill for any shift, and the reverse pass is a contiguous copy instead of the scatter a gather-based translation would pay. Second derivatives: reverse-over-reverse only (the fwd rule recomputes its primal inline so `jacrev(jacrev(...))` stays exact; forward mode is rejected by `custom_vjp`, and `jax.hessian`'s forward-over-reverse was independently measured asymmetric on this stack even for plain autodiff — `laplace_inverse_mass` switched to rev-over-rev, fixing a defect present since M3). Remaining levers on the ledger, not implemented: analytic VJP through the assembly itself, custom-VJP band→block packing, associative-scan (parallel-prefix) Cholesky for GPU chain-latency, opt-in mixed precision, fusing the Takahashi sweep into cotangent assembly (design-target grad is memory-bound at 32 GB) | benchmarks.md M5 speedup pass; band-vs-probe and VJP-vs-autodiff equality are regression-tested in tests/test_assembly.py |
| D29 | Memory pass + boundary correctness (post-D28) | The design-target gradient measured **34.0 GB** against 32 GB of RAM — it did not fit. Four reductions, all exact: (1) the Takahashi sweep now emits the cotangent blocks directly (`solver.selected_inverse_cotangent`), so the selected inverse is never materialized; (2) the velocity-independent `G` pre-pass is **batched over epochs** (`epoch_chunk`) — `vmap` batches every intermediate of the chain, not just the result, so the un-batched pre-pass alone was ~9 GB; batching costs one extra `G` pass in the backward and is therefore applied only above a ~1 GB threshold, leaving small and gate-scale problems on the fast path; (3) the prior determinant uses a scalar pentadiagonal recursion (`assembly.prior_logdet`) instead of a block-tridiagonal factorization of a bandwidth-2 matrix; (4) `_pack_band`'s gather indices are hoisted out of the `lax.map` body (they never depended on the block counter) and the body is rematerialized, so reverse mode stops stacking (B,B) index and mask arrays over all K blocks. Result at the design target: eval 20.6 → 11.1 GB, gradient 34.0 → 18.2 GB, and — because the gradient had been thrashing against RAM — eval 26.0 → 22.2 s and gradient 111.3 → 87.4 s with it. (Mid-ladder gradients pay 5–23% for the batched pre-pass's extra backward `G` pass, where memory was not the binding constraint; `epoch_chunk` trades it back.) **Correctness fixed in the same pass**: the LSF convolution wrote `G` band entries at column indices outside the model grid and the T-sandwich read them, because only the sandwich's *row* index was zero-filled. Trigger: data-coverage margin (in model pixels) below the kernel radius — which includes choosing a model grid narrower than the observed range to fit a sub-region, and is reachable at a *fixed* grid just by fitting a wider LSF. At zero margin the assembled precision was wrong by 6.8e-2 relative and **asymmetric** by the same amount (probing is symmetric), worth −57 nats of log-likelihood. Every fixture happened to leave a margin exceeding the kernel radius, so the weights vanished where the defect lived. `G`'s out-of-grid columns are now masked at the source. Also: the Cholesky factor left `_solve_stage`'s outputs — a cotangent on it cannot be honoured by the closed-form rule, so `spectra_std`/`draw_spectra` gradients were silently **zero**; `MarginalResult` now stores the precision and rebuilds the factor outside the custom boundary (one extra Cholesky, paid only by callers that want it). `build_problem` warns when a few anomalously wide native pixels set `row_support` — deleted telluric/order gaps rather than masked ones, which inflates the solver bandwidth quadratically | benchmarks.md "D29 memory pass"; the boundary case is now a fixture (`edge_covered`) compared **entrywise** against probing, since the log-determinant and solve it previously guarded average the defect away |
| D30 | Real-data ingestion path (first observed dataset: HR 6819) | Two new modules and four fixes, driven by putting 51 archival ESO Phase-3 FEROS spectra through the stack. **`albireo.preprocess`** (pure NumPy, like `data.py`): `fit_continuum` / `normalize` — an asymmetric-envelope-then-asymmetric-sigma-clip penalized fit **of `log(flux)` on a knot grid**, because a continuum is multiplicative (the measured FEROS response falls 20× over 3850–4750 Å, which a curvature penalty in linear space cannot track — normalized flux came out 30% wrong) and because a per-pixel Whittaker smoother needs `lam ~ (L_px/2π)^4 ~ 1e12` at echelle smoothing lengths, at which the weight term is lost to rounding and the SPD factorization *fails outright*; knots at 8 per smoothing length hold `lam ~ 2.6`. `estimate_ivar` — DER_SNR (Stoehr et al. 2008, ESO's own recipe) in wavelength bins, fitted to `sigma^2 = s^2/continuum`, because the ESO FEROS `ERR` column is **entirely NaN**. `select_region` / `mask_ranges` / `mask_tellurics` / `mask_spikes` — masking is always `ivar = 0`, never deletion (D29's row-support trap). `share_wavelength_grid` — index-aligned (never value-matched: a `searchsorted` at a window edge slips a whole 1.4 km/s pixel) relabelling of per-exposure grids onto one, asserting the residual is within tolerance; a *relabelling*, not a resampling, so D4 stands. **`albireo.io`** (optional `astropy`, `[io]` extra): `read_spectrum` for ESO Phase-3 binary tables and WCS image spectra, resolving frame from `SPECSYS`, `v_bary` from the pipeline's own keyword (`ESO DRS BARYCORR` and friends — the pipeline's value is what *defines* the delivered frame; astropy only as fallback, agreement measured at 0.017 km/s), and BJD_TDB at mid-exposure from the extension-header `TMID`; every assumption it has to make emits a warning naming it. Plus `LogGrid.covering`, which sizes the model-grid margin as shift + kernel radius + slack. **Fixes found by the data, all with regression tests:** (1) `build_problem` formed `z = flux - r·base` unmasked, so a non-finite flux at a zero-weight pixel — which `data.py` explicitly *documents as legal* and which `normalize` produces at a collapsed continuum — took the whole marginal likelihood to `nan` via `0 * nan`; (2) epochs of one instrument had to share a bit-identical `wave` array or `build_problem` raised, but a pipeline that shifts before resampling gives one grid per exposure (51 spectra → 28 grids), so grouping is now by (instrument, grid) with the instrument key still keying the LSF, and `_epoch_chunk_default` divides its byte budget by the group count; (3) `prior_logdet`'s Cholesky pivot returned `nan` below `eta/tau ~ 1e-13`, reachable by unbounded ML-II, now floored; (4) the row-support warning seeded its minimum with `int64` max and never restricted to rows the operator touches, so a region-selected epoch produced a warning quoting nonsense. `run_map` gained a `callback` (it was silent for hours, and its absolute `tol` is unreachable at survey pixel counts). **Not fixed, recorded:** light fractions remain an input for a non-eclipsing system (D13 — the K-band GRAVITY ratio does not transfer to 4400 Å), there is still no jitter site (D15) so estimated inverse variances must be checked against `data_residual_zscores` (**built afterwards — see D31**), and the numpyro path captures `Problem` as a closure constant rather than a jit argument, so it pays the XLA constant folding D27 avoids for `MarginalOrbitModel.marginal` (**fixed in D32**) | benchmarks.md "HR 6819"; `tests/test_preprocess.py`, `tests/test_io.py`, `examples/03_hr6819_real_data.py` |
| D31 | Jitter site built (closes D15) | `EpochGroup` carries a per-epoch factor α_j; **every consumer of the weights reads `effective_w = w/α²`, none reads `w`**, so the inflation reaches the normal matrix, the right-hand side and the `Σ log w` term together — which is exactly what makes α identifiable instead of a free knob. Swapped in by `forward.with_jitter` (scalar or per-epoch, traced-safe, *replaces* rather than compounds, since the raw `w` is kept) and exposed as the optional θ site `log_jitter`. **What the marginal actually estimates:** in the data-dominated limit `−½ log det(Λ + AᵀWA)` contributes `+p_eff·log α` against the weight term's `−N·log α`, so profiling gives `α² = χ²/(N − p_eff)` with `p_eff = tr[(Λ + AᵀWA)⁻¹AᵀWA]` — the *effective-dof-corrected* variance estimate, not `χ²/N`. The naive route (whiten the residuals, read off their standard deviation, rescale ivar) is low by `√(1 − p_eff/N)` — 4.6% in the weak-prior fixture. **How much it matters is a property of the run, and `p_eff` is emphatically not `n_comp · n_pix`**: on HR 6819 an oversampled grid plus a fitted smoothness prior gave `p_eff ≈ 2900` against 19,876 model pixels — about the *resolution-element* count — so there the correction was 0.4%. Inverting the two estimators, `p_eff = N[1 − (sd/α̂)²]`, is a free and otherwise awkward diagnostic of how much of the spectrum the data actually constrain. Regression-tested by injecting a known ivar scale error and recovering it to 0.8%. **Deliberate limits, documented at the function:** a diagonal inflation cannot represent a residual correlated across pixels, so a jitter fitted against systematics (continuum, LSF mismatch, line-profile variability) reports a *wider but still biased* orbit; `data_residual_zscores` now whitens by the assumed weights, jitter included, so after fitting one it reads ≈1 by construction and the diagnostic that still bites is the residual's shape, not its scale. **Measured on HR 6819, and the result is a warning, not a win**: per-epoch α runs 1.11–3.61 (the worst exposure ends up with 1/13 of its nominal weight, and it buys +19,763 nats over one shared α), the residuals whiten to sd 0.997 exactly as advertised — and the period *moves by 174× the no-jitter formal error*, away from the published value, because the noisiest exposures cluster in the first third of the baseline (`corr(α, phase) = −0.25`) and downweighting them removes the period leverage at one end. Both fits are genuine optima under their own weights (cross-evaluated: +10,622 nats one way, +3,835 the other), so this is the noise model *selecting* the optimum, not a defect. Window-to-window disagreement in period went from 5.6× to 13.1× the combined formal error. The standing lesson gains a clause: quote the spread across independent windows **and across defensible noise models** | `tests/test_jitter.py`; math.md §3.2a; benchmarks.md "The jitter site" |
| D32 | numpyro path traces the problem (closes D30's recorded leftover) | `MarginalOrbitModel.model()` takes the base `Problem` as an optional model argument and advertises it via a `model_args` attribute on the returned closure; `run_map` / `laplace_inverse_mass` / `run_nuts` resolve the arguments (explicit > model attribute > none) and thread them through numpyro as *traced* jit arguments — `initialize_model(dynamic_args=True, model_args=...)` for the MAP/Laplace potential, `MCMC(jit_model_args=True)` for the NUTS sample loop — extending the D27 contract to every jitted entry point, with zero call-site changes. Measured on the m5 ladder (50 epochs, CPU, value+grad of the potential): compile 20.9 → **0.8 s** at 31.7k px and 10.8 → 0.8 s at 74.3k px; the closure build folded **1.99 GiB** of derived constants into the executable at 31.7k px (+1.35 GiB process peak at compile, XLA's slow-folding alarm firing on `f64[50,126936]` scatter-adds), while at 74.3k px XLA's folding guards declined the largest folds — the memory cost is heuristic-gated across scales and versions, which is an argument for removing the exposure, not for betting on the guard (D27 measured ~80 GB from the same mechanism at the design target). Results are unchanged (potential/gradient agree to all printed digits; run_map paths agree to float tolerance, Laplace exactly). A model called with *no* argument (plain numpyro utilities, `log_density`) falls back to the closure — correct, just not compile-safe at scale; `model_args=()` forces it deliberately | benchmarks.md "D32"; `tests/test_inference.py` jaxpr-consts probe + path-agreement tests; `scripts/d32_model_args_bench.py` |
| D33 | Response θ-site (closes D7's deferral — the per-epoch continuum treatment) | `forward.with_response` + optional θ site `response` (`(n_coef,)` shared or `(n_epochs, n_coef)` per-epoch, layout as the `light` site). The response enters the *targets* `z = y − r(R·1)` and the sandwich weights `w r²`, not just the operator — D7's reason for deferring — but `R·1` is response-independent and now stored per group, so `z_new = z_old + (r_old − r_new)·R·1` rebuilds the target exactly with no raw fluxes carried (re-masked, so D30's `0·nan` trap cannot resurface); `Σ log w` is untouched (the noise lives on the data); *replaces* rather than compounds, like the jitter. The traced Clenshaw matches `np.polynomial.chebyshev.chebval` operation-for-operation: the swap equals a fresh build with `r` **bitwise** equal, marginal rtol 1e-12. **Identifiability, measured** (§5's response row): closed loop recovers the epoch-to-epoch coefficient *differences* to 2.0e-3 rms against injected 3.4e-2 with K to 0.2%, while the epoch-shared mode lands at its zero-centered prior, not at truth (c₀ error −0.069 against prior σ 0.05) — keep order low, priors tight, and read the common mode as a normalization convention. **On HR 6819 the site exonerates the continuum**: +4,100 / +3,926 nats (windows A/B) absorbed by per-mil coefficients (difference mode 5e-4 rms in both windows), and *nothing else moves* — period +0.0004/−0.0002 d (0.4σ/0.2σ formal, against the jitter's 174σ relocation), K by <0.001 km/s, residual sd −0.4% (1.674→1.668, 1.401→1.394), window-to-window spread unchanged (ΔP 2.8σ→3.1σ, ΔK 10.5σ). The D30 suspect list loses "per-epoch continuum residuals"; the survivors are the pipeline resampling's pixel correlations, the Gaussian LSF stand-in, and disc variability — so the recorded next steps are a correlated-noise model and a wider window. Bonus sighting: window B's uniform-procedure baseline MAP sits 0.0093 d (6.6× combined formal σ) from the D30-recorded window-B period — multimodality demonstration #3, and the reason the comparison only pairs fits sharing one procedure | benchmarks.md "D33"; `tests/test_response.py`; `scripts/hr6819_response_run.py`; math.md §7.5 |
| D34 | Correlated noise: per-epoch AR(1) chain (the top surviving D30 suspect — the pipeline's resampling) | `C = α²·D^(−1/2)·R_φ·D^(−1/2)` (math.md §1.4a): the correlation matrix has **unit diagonal by construction**, so supplied per-pixel variances are kept and the residual-*sd* diagnostic is provably blind to φ — the discriminator is the lag-1 autocorrelation of the whitened residuals (~φ under diagonal whitening, ~0 under the chain whitener). A subset of a Markov chain is Markov, so masked pixels are **exact, not approximated**: each gap collapses to a single link with `ρ = φ^gap` (capped at build, `ar1_max_gap=4`, beyond which the chain restarts); the noise precision stays tridiagonal with closed-form `log det` and an innovation whitener, and every consumer of the weights reads it (normal matrix, rhs, quad, `Σ log`-terms, `data_residual_zscores`) — the D31 identifiability argument again. Swapped in by `forward.with_ar1` (scalar or per-epoch, *replaces* like the jitter), θ site `ar1_phi`, behind an explicit `MarginalOrbitModel(ar1=True)` opt-in because the chain couples across masked gaps and the solver bandwidth grows by a statically reserved `ar_bandwidth_extra` (D21's declared-bandwidth philosophy; 5–6 model px on HR 6819). Correlated problems are **rejected by the D28 band assembly** — it assumes diagonal weights — and auto-select the probe path: ~15× the D33 per-step cost measured on HR 6819; the tridiagonal band-sandwich extension is a recorded lever, to be built only if AR(1) earns a permanent place. Exactness against a dense reference sharing nothing with the closed forms (chain *correlation* matrix from link products, LAPACK-inverted): marginal **rtol 1e-10** with gaps and jitter composed, φ=0 ≡ diagonal model at rtol 1e-12, FD gradients including exactly at φ=0 (`jnp.power`'s nan-gradient at zero base — gap-1 links use φ directly). Closed loop: φ=0.45 *and* a 1.5× ivar overstatement injected together, recovered jointly with the orbit — φ̂ 0.4493, α̂ 1.4868, K to 0.31%. **On HR 6819 the chain is the first noise model in the campaign to whiten both moments — and the orbit stays put**: φ̂ +0.801 / +0.694 (windows A/B), chain-whitened sd/lag-1 0.997/+0.04 and 0.997/+0.01 (the same residuals under the diagonal whitener read lag-1 +0.797/+0.688 — φ̂ to two decimals), and the period lands at 40.3712/40.3696 — the D31 relocation (40.444/40.430) **does not recur**: the per-epoch jitters collapse from 1.11–3.61 to 1.55–1.93, their medians (1.66/1.40) reproducing the D30 residual sds (1.674/1.403) — D31's jitters were fitting correlated structure as per-exposure scale, and modeled as correlation the excess is roughly uniform across epochs, a pipeline property. The independent windows move **3.0× closer in period** (ΔP 0.00475 → 0.00159 d; D31 had widened it to 0.0145 d) — the first noise model that makes the windows agree more, not less. K_Be moves toward the literature (1.93→2.45, 2.95→3.76, vs 3.90±0.27; window B now within 1σ). The literature period offset (~0.044 d) survives its third noise model — measured not-continuum, not-scale, not-correlation; survivors are the Gaussian LSF stand-in, disc variability, and the published CCF blending itself. At φ = 0.7–0.8 a diagonal model overcounts low-frequency information by (1+φ)/(1−φ) ≈ 5.5–9× — the mechanism behind D30's ±0.0005 d formal errors coexisting with 0.005 d window disagreements | benchmarks.md "D34"; `tests/test_ar1.py`; `scripts/hr6819_ar1_run.py`; math.md §1.4a |
| D35 | Band assembly extended to the AR(1) chain (pulls the lever D34 recorded; prerequisite for the wider window) | D34's condition — build the tridiagonal sandwich only if AR(1) earns permanence — was met by its science verdict, and the wider window could not be paid for on the probe path (gradient peak **23.7 GiB at current window scale** on a 32 GB machine). The only stage of the D28 assembly that assumed diagonal noise was the innermost sandwich `H = R^T W' R`; the chain adds one symmetric *cross-row* term per link, `−c·√(w_n w_p)·r_n r_p·(R_n^T R_p + R_p^T R_n)`, and re-weights the diagonal by the chain diagonal `1 + Σa` (math.md §4.5a). Carried by a second set of static pair tables (`operators.rebin_link_pair_tables`) built at `build_problem` time over the **union of links realized in any epoch** — masks differ per epoch, so a per-epoch gap test (`ar_gap[e, link_row] == link_gap`) selects each epoch's own links against the shared tables; restricting the tables to realized links is also what keeps them inside the `ar_step` width bound. Upper-band storage gets both transposes from the two orderings of each entry pair, doubled where they coincide on the diagonal. `H` widens by the group's static `ar_step` — the same quantity `ar_bandwidth_extra` already reserved — and **everything downstream is untouched** (LSF convolutions, T-sandwich, band accumulation, D29 chunking, custom-VJP solve). `marginal_loglikelihood` auto-selects `"band"` unconditionally; probing stays as the reference implementation and `validate` oracle. Exactness: the D34 dense-LAPACK gold test runs the band path at rtol 1e-10 unchanged; band = probe at rtol 1e-12 with ∂/∂φ to 1e-9; `epoch_chunk` batching invariant (the AR weight tuple — diagonal, link, gap — pads and slices together; a batched run dropping link terms would fail silently, so it is pinned). Measured at HR-window scale (51 ep, 9.8k model px, 367k native px, CPU): eval 5.10 → **0.71 s** (7.2×), value+grad 58.83 → **3.07 s** (19.2×), gradient peak working set 23.7 → **1.85 GiB** (12.8×), results identical to all printed digits — the D28 story again: the band path's custom-VJP solve vs plain reverse mode through 2p+1 = 347 probe applications. Window A refit unchanged as the real-data proof: **8,369 → 868 s end-to-end (9.6×)**, same optimum — loglike 1,691,463.5 to the printed digit, P within 0.00002 d (0.02 formal σ), φ̂ +0.801, α range and residual diagnostics identical | benchmarks.md "D35"; `tests/test_ar1.py`; `scripts/d35_ar1_band_bench.py`; math.md §4.5a |
| D36 | The wide window: one joint HR 6819 fit, 4120–4600 Å with the Hγ core masked (the last lever D30/D33 recorded; spends the D35 speedup) | Joins windows A and B — 22,169 model px, ~765k good native px, 2.26× window A, including the never-fitted 25 Å strip 4355–4380 — with Hγ's core (4325–4355) masked by `preprocess.mask_ranges`: ivar = 0 keeps the sampling regular, the AR(1) chain restarts across the hole (masked gap beyond `ar1_max_gap`), the bandwidth is untouched, and the broad absorption wings (static stellar features) stay in. D34 noise model and procedure exactly; 200 L-BFGS steps, **2,447 s at 12.2 s/step** on the D35 band assembly — linear-in-pixels from the window A refit, where the probe path would have priced this at ~7 h against a ~50 GB gradient. Results: **P 40.36750, K 63.396/3.482, e 0.0261, φ̂ +0.737, α 1.46–1.86 (med 1.61), chain sd/lag-1 0.997/+0.019**, diagonal lag-1 +0.731 ≈ φ̂ (self-consistency, a third time). Readings: the noise model closes at 2.3× the data, with φ̂ and α̂ between the single-window values (a pipeline property varying mildly with wavelength); **K_Be firms toward the literature** — 3.48 vs 3.90 ± 0.27 (1.5σ), where window A alone said 2.42: the §5.1 data-starved direction responded to more lines; e at 0.5σ; the period lands **below both single-window optima** (40.3675 vs 40.3711/40.3696) — a joint fit is not an average of window MAPs, and the multimodality lesson applies — moving toward the literature, still 0.041 d away. **The literature period offset survives its fourth configuration**: two windows, three noise models, one wide joint fit give P ∈ [40.3675, 40.3712] (internally consistent to 0.004 d) vs published 40.3261 ± 0.0013, K_pre-sd 63.2–63.5, consistently 3.7–4% above the CCF value in the deblending direction. Survivors: the Gaussian LSF stand-in (next lever; D8's tabulated-LSF v2 seam — a banded-operator swap), disc variability, and the published CCF blending itself | benchmarks.md "D36"; `scripts/hr6819_wide_run.py` |
| D37 | The tabulated-LSF seam opened (D8's v2; the top surviving D36 suspect) | The kernel slot becomes a **profile bank**: one row = the stationary v1 operator (bit-identical code path), `grid.n` rows = a wavelength-dependent LSF — per-anchor kernels linearly interpolated in log-λ through static tables (`operators.lsf_anchor_tables`), applied by the row-varying convolution `operators.convolve_varying` (`K[m,c] = P[m, m−c+r]`; exact adjoint; **arbitrary asymmetric banks accepted** — the θ-parameterization is per-anchor Gaussian widths, `lsf_sigma` one entry per anchor with build widths as per-entry radius bounds). The band assembly keeps its structure: scalar sandwich taps become row-shifted profile columns, and since only *left* applications broadcast on a row-major band image, the second application runs against the band-transpose of the first — `G = Kᵀ(KᵀH)ᵀ` by G's symmetry (math.md §4.5); same width, same flops. Band == probe == dense at rtol 1e-12/1e-10 under diagonal *and* AR(1) noise; per-anchor gradients band == probe at 1e-9; random asymmetric banks pin any hidden kernel flip; simulator injects through the same recipe. **The closed loop measured the identifiability honestly** (math.md §1.3): a stationary kernel change commutes with the shifts and is absorbed by the free spectra, so the marginal does *not* prefer an injected width ramp over a flat width (−3 nats) — fitted widths are **diagnostics, not measurements**; the identified content is the anchor-to-anchor variation and the orbit's response, and the joint fit leaves the orbit unbiased while recovering the ramp's direction. **On HR 6819 (13 anchors × 40 Å on the D36 configuration): +90.5 nats and the orbit does not move.** P 40.36769 (+0.0002 d = 0.5% of the offset), K₁ 63.395, e 0.0262, φ̂ +0.737, α̂ and both residual moments unchanged to the third digit; σ̂(λ) reads 2.15 at the blue edge then 3.1–3.4 hugging the 3.5 bound (the closed-loop absorption behavior on real data); K_Be unconverged at 200 steps (oscillating 1.30–4.16, a band spanning D36's 3.48 — the 13 near-flat width directions slow the flattest axis; 28.3 s/step, 2.3× D36). D33's pattern, now for the LSF. **The literature period offset survives its fifth configuration**; LSF *width* variation is exonerated, and the surviving LSF suspect narrows to profile **asymmetry** (first-order centroid channel ∝ λc′(λ)v(t)/c; the operator is ready, only a θ-parameterization — e.g. per-anchor Gauss–Hermite h₃ — would be new), alongside disc variability and the published CCF blending | benchmarks.md "D37"; `tests/test_lsf_varying.py`; `scripts/hr6819_lsf_run.py`; math.md §1.3, §4.5, §7.5 |
| D38 | The asymmetry lever: per-anchor Gauss–Hermite h₃ (closes D37's surviving LSF suspect) | The θ-parameterization for the operator D37 left ready: `operators.gauss_hermite_kernel_traced` (van der Marel & Franx 1993 truncated at H₃, unit sum, **h₃ = 0 bit-identical to the Gaussian**), threaded through `gaussian_lsf_profiles`/`build_problem`/`with_lsf` for anchored instruments only — a *stationary* asymmetry is absorbed by the free spectra, so un-anchored instruments get no slot — with site `lsf_h3` (per-anchor, concatenated across anchored instruments, clipped + guarded at \|h₃\| ≤ 0.2, may appear without `lsf_sigma`). Exactness: band == probe == dense with h₃ anchors under diagonal and AR(1) noise; ∂/∂h₃ band == probe at 1e-9. **The closed loop measured the identifiability, and it is sharper than the width case**: an injected ∓0.12 h₃ ramp came back flat (fitted \|ĥ₃\| ≤ 0.03) with the orbit recovered to 1% — a free spectrum represents any static centroid-warp field c(λ) ≈ √3 h₃ σ outright; the data-identified remainder is the epoch-coupled warp-gradient sampling Δc ≈ c′λ(v − v_bary)/c ≈ **30 m/s** here (math.md §1.3 records the estimate, and why the instrument-frame per-epoch kernel realization is bounded out rather than built — the channel it adds is two orders below the offset's ~4 km/s accumulated signature). **On HR 6819 (13 h₃ anchors joint with the D37 widths, 300 steps, 36.0 s/step): +26.6 nats and the orbit does not move** — P 40.36719 (−0.0005 d from D37, inside the trajectory wobble), K₁ 63.391, e 0.0261, φ̂ +0.737, both residual moments unchanged; ĥ₃ interior ≈ 0 with 0.04–0.05 at three anchors (centroid shifts ≤ 0.31 km/s, diagnostics); K_Be unsettled again on its flat axis (2.73 at \|grad\| 44). **The offset survives its sixth configuration and the LSF is exonerated in full**: every instrumental channel this model can express — continuum (D33), noise scale (D31), pixel correlation (D34), window (D36), LSF width (D37), LSF asymmetry (D38) — is measured not to move the period (P ∈ [40.3672, 40.3712] vs published 40.3261 ± 0.0013). Surviving suspects are no longer instrumental: the Be disc's variability and the published CCF blending. The instrumental-systematics campaign on this dataset is complete | benchmarks.md "D38"; `tests/test_lsf_h3.py`; `scripts/hr6819_h3_run.py`; math.md §1.3, §7.5 |
| D39 | The usability layer: results, plotting, packaged example data (the Tier-1 roadmap block) | Six defaults worth recording so they are not relitigated. **(1) Serialization is `.npz` with a JSON header, not netCDF or HDF5.** All four result types are "flat arrays plus a few scalars", so `load_fit` reconstructs by calling the constructor with keywords — no pickling, and no pytree aux-data serialization to keep in step with the classes. netCDF would pull in xarray + h5netcdf and FITS would pull in astropy; NumPy is the only hard dependency that can write a container. A `__format_version__` in the header makes a future reader refuse rather than misread. **(2) `MarginalResult` saves `d_hat` + `spectra_std` by default, not the precision** — the blocks are GB-scale at the design target (D29), and a default that quietly dropped the uncertainty band would drop the differentiator; `precision=True` round-trips exactly. Consequently `K2ScanResult.model` is now optional: a scan read back from disk has no live `MarginalOrbitModel`. **(3) matplotlib and arviz join astropy as optional extras**, lazily imported through the package `__getattr__` so `albireo.plot_spectra` stays discoverable and raises an actionable message rather than an ImportError from inside a figure — a fit on a headless node should not need a plotting stack. A `bare-install` CI job installs with no extras and asserts the guard, because an untested guard is an intention. **(4) The packaged example is deliberately eccentric (e = 0.15), not circular.** The (√e cos ω, √e sin ω) parameterization is singular at exactly e = 0: ω is undefined, e·cos ω behaves like \|x\|, and the gradient is NaN at the origin — numpyro then reports "Cannot find valid initial parameters", which does not point at the cause. A circular example would put both the truth and the obvious starting guess on that point, so the first thing a new user runs would fail. Documented in the quickstart; a guard in `run_map` is a recorded lever, not built. **(5) The example ships inside the wheel** (124 kB, seeded, regenerated by `scripts/build_example_datasets.py`) and the cache/download path is stdlib `urllib` + `hashlib`, not `astropy.utils.data` — `load_example` is the *first* call a new user makes, and requiring an extra for it defeats the purpose. **(6) A bare `pytest` still runs the acceptance gates**; `--no-slow` deselects them (14 min → 6 min) and CI's matrix passes it while one job runs everything with coverage. The gates are why the closed-loop tolerances can be trusted, so the default that quietly stopped checking would be the wrong default. Two pre-existing defects surfaced, both invisible because nothing had ever been pushed and CI had therefore never run: `pytest` (the console script) could not collect three modules that import shared helpers as `tests.test_likelihood`, and `ruff format --check` did not pass on a clean tree | benchmarks.md is unchanged (no numerics moved); `tests/test_results.py`, `tests/test_plotting.py`, `tests/test_examples.py`, `tests/test_metadata.py`; roadmap.md; releasing.md |
| D40 | The nebular component, and per-pixel prior strengths (Tier-2 roadmap item 1) | **The component.** `build_problem(nebular=True)` appends a column that is static in the *barycentric* frame — the mirror of the telluric law, so it is the topocentric-frame data that carry the per-epoch `-v_bary` term — with a **free per-epoch amplitude** in place of a light fraction. That last point is the physics, not a convenience: nebular flux is *added on top of* the total stellar continuum and takes no light from the stars, so its amplitude does not belong on the simplex, and its night-to-night variation (seeing, slit losses, sky subtraction) is one scale on one fixed shape. Structurally it is rank-one time variation — the same shape Tier 3's variable-disc component generalizes, which is why that generalization stays inside the linear-Gaussian family. Swapped by `forward.with_nebular_amplitudes`, θ site `log_nebular_amp`. Component order is now stellar, telluric, nebular, and the two θ-path swaps that used to *rebuild* the telluric column now **carry the trailing columns through unchanged** — shorter, and correct for any number of non-stellar components, since their velocity laws depend only on the frame and `v_bary`. **Two degeneracies, both closed by convention rather than by data.** *Scale:* only `a_j·d_neb` is observable, so the prior would be left breaking a nearly flat unbounded direction — the worst of both worlds. The site is centered, `a_j = exp(u_j − mean u)`, pinning the geometric mean to 1; the prior on it is then a prior on epoch-to-epoch *variation*, and the level lives in the spectrum. *Velocity:* the nebular shift is epoch-constant (barycentric) or differs only by `v_bary` (topocentric), and a constant shift of a free spectrum is a reparameterization — so `nebular_v_kms` is **not identified**, exactly as γ is not (D14). It survives only because it decides where the component's lines land on the model grid, which is what the window profile below has to agree with. Both are stated at the call sites rather than left for a user to rediscover. **The prior grew a per-pixel axis** because confinement needs it: `SmoothnessPrior` takes optional static `tau_profile`/`eta_profile` multipliers, with the inferred scalars kept separate so ML-II is untouched (a profile says *where*, the scalar says *how much*). The pentadiagonal entries generalize to `d0 = t_a + 4t_{a-1} + t_{a-2} + e_a`, `d1 = −2(t_a + t_{a-1})`, `d2 = t_a` with curvature rows weighted by their center pixel — same bandwidth, same O(P) determinant recursion, same band assembly, and bit-identical to v1 at uniform profile. Because the split is *structure vs. strength*, an inferred `(tau, eta)` **keeps the construction-time profiles** — `MarginalOrbitModel` merges the sampled scalars into the fixed prior's profiles rather than replacing the whole object, which is the difference between a windowed component and one that is silently un-confined the moment ML-II is switched on. `priors.window_profile` + `nebular_windows` + `NEBULAR_LINES` build the confinement: `eta × 1e6` outside the line windows pins the component to the continuum there (prior sd 1e-3 of the in-window value) while leaving the precision *better* conditioned. Deliberately soft — a hard zero needs different linear algebra and, worse, removes the model's ability to disagree. **Measured, not asserted.** Closed loop: an SB2 (K = 58/41 km/s, ℓ = 0.7/0.3, 12 epochs) whose Hβ absorption carries a static nebular line of peak 0.45 varying ±30% per epoch. Disentangled *without* a nebular component the mean flux in the line core rises by **+0.154** against a true depth of −0.506 (the recovered core bottoms out at −0.375, **26% shallower**) and the Hβ **equivalent width comes out 11.5% low** (1.690 vs 1.911 Å). With the component: **+0.0015**, −0.508, and **0.14%**, marginal likelihood ahead by **8.1×10⁴ nats**, recovered amplitudes correlating with the injected ones. The EW number is the one that matters, because equivalent width is what reaches the atmosphere code: this is a systematic error in log g that nothing in the current literature propagates. **And the orbit is the sharper result, which was not expected going in**: the same data fitted jointly from a cold start gives K₂ **16.77 vs 41.0 (−59%)**, P **+0.171 d**, and a circular orbit reported at **e = 0.95 — the solver's clip** — without the component, against K₂ **−0.29%**, ΔP −0.00014 d, e 0.0022 with it; the blind fit is also still at \|grad\| 2.9e4 where the modelled one has settled at 8.3, and takes 3.6× the wall time to fail. A static line is a component with K = 0, so a model with nowhere to put it uses whichever star can be made to move least; only K₁ survives, pinned by 70% of the light. The nebular contamination therefore propagates into the *dynamical* answer — masses — and not only into the atmospheric one. Per-epoch amplitudes recover at **corr 0.99930, 0.0066 rms in log** against an injected 0.78–1.50× spread, and ML-II independently keeps the nebular component less smooth than the stellar ones. **The window profile earns its keep in that same fit**: free across the whole grid the component takes K₂ to +2.6% and the potential 250 nats worse — the freedom it was given was being spent absorbing stellar signal where a nebula has no lines, which is the original failure one level up. The forward model reproduces the simulator's injection to 1e-12 in both frames, and band == probe with the nebular column and a window profile present | benchmarks.md "D40"; math.md §1.3, §2, §5.4; `tests/test_nebular.py`; `examples/04_nebular.py`; roadmap.md Tier 2 item 1 |
| D41 | Calibrated faint-companion detection: a vectorized scan, K₁ marginalized, and an empirical false-alarm rate (Tier-2 roadmap item 2) | **Vectorization first, because the other two are unaffordable without it.** `MarginalOrbitModel.log_likelihood_sweep` runs a grid of trial θ values as a single batched `lax.map` — one compiled graph, no per-point device synchronization — with the same two-regime batch policy `_epoch_chunk_default` uses for memory. Measured **2.0-2.8×** across problems from 201 to 2,652 model pixels — near-flat in size, so it is the batching rather than the dispatch removal that pays — agreeing with the loop to 1e-12 relative or better. The point is not the factor but what it buys: a 7×20 (K₁, K₂) grid costs 0.88 s and a 450-scan calibration (9,450 marginal solves) costs 53 s. **K₁ is a nuisance parameter, and conditioning on it is not free.** `k2_scan(k1_sigma=)` integrates it out over a Gauss–Hermite rule on N(μ₁, σ₁²), applied to the companion **and** the no-companion model so that D remains a ratio of two marginal likelihoods rather than a comparison of differently-conditioned ones; `k1_sigma=None` is the one-node rule and reproduces the previous behavior exactly. **The measured failure mode is worse than the literature's description of it.** The papers report that a small error in the assumed primary semi-amplitude puts spurious features in the recovered secondary. What the closed loop shows is that D *rises* while that happens — unremoved primary signal is coherent across epochs, the companion's free spectrum is the only thing that can absorb it, and it does: K₁ 5% high took the companion's recovered line pattern from **0.96 to 0.72 correlation with truth while D went 40,609 → 66,837**, and K₁ 10% high gave **0.49 and D = 135,410**. The artifact reads as a *stronger* detection. Marginalizing restored 0.955 and 0.931 with D back at ~41,000, and recovered the correct K₂ peak where the fixed 5%-high scan had it one grid step low. Peak spectra stay conditional on the best node (a profile): there is no closed form for the K₁-marginalized spectrum, and averaging the nodes' spectra would blur the lines instead of widening their error bars. **The calibration is a parametric bootstrap through the observed data's own operators.** `forward.with_data` swaps the data term z = y − r(R1) — the only place the fluxes enter — and `simulate.resimulate` redraws it from the problem's own forward model, so epochs, v_bary, chip gaps, cosmics, native wavelength solutions, response and per-pixel weights are the real ones and each trial costs one forward apply instead of a fresh `build_problem`. `calibrate.detection_limit` scans N companion-free draws for the null distribution of **max over the grid** (the statistic a search actually reports, not D at one trial K₂) and a ladder of injected ℓ₂ for completeness. **Two properties are enforced rather than hoped for.** The threshold is defined *through* the FAP estimator (1 + #{null ≥ D})/(N+1) rather than as a sample quantile: `np.quantile` interpolates between order statistics and can leave 8% of the null above a nominal 5% threshold — caught by a test, and anti-conservative in exactly the direction that matters for a detection claim. And no FAP below 1/(N+1) is ever reported; when the requested rate is finer than the trials resolve, the rule degrades to 'must exceed every null trial' instead of inventing precision. A ladder whose faintest rung is already complete sets `limit_is_bracketed=False` and `summary()` says *at most*, because reporting the first rung would look identical to a measured crossing. **What this deliberately does not do is check the model.** The null trials are drawn at the same K₁, orbit and light fractions the scan assumes, so the threshold is self-consistent with those assumptions and blind to their being wrong — which is precisely why the K₁ marginalization exists beside it. The limit is also conditional on the assumed companion template (the observable is ℓ₂·d₂, so a featureless companion is invisible at any light fraction); the default is the primary's own recovered spectrum, and the assumption is required to be quoted with the number. Measured on a 717-pixel, 14-epoch, SNR-200 SB1: null peaks strictly negative (the Occam term, so 'D > 0' would have been a *conservative* test here and on another dataset might not be), and the search sensitive below **0.5% of the light** at 95% confidence | benchmarks.md "D41"; math.md §6.1, §6.2; `tests/test_calibrate.py`; `examples/05_detection_limit.py`; roadmap.md Tier 2 item 2 |
| D42 | The free per-epoch radial-velocity table (Tier-2 roadmap item 3) | A ``velocity`` theta site of shape (n_stellar, n_epochs) that **replaces** the Keplerian rather than supplementing it (mixing the two raises), turning the joint fit into the diagnostic mode the design has listed since M3 and never exposed. `forward.with_shifts` was split out of `with_velocities` as the pixel-space core, and `LogGrid.pixels_to_velocity` added as the exact inverse of `velocity_to_pixels`. **The identifiability is the whole decision, and it is not what it looks like.** A free table has one arbitrary zero point *per component*, not one in total: with no orbit tying the components together, each component's free spectrum absorbs a constant added to that component's shifts. So this is gamma (D14) once per star. Measured directly before building anything on it: a whole-pixel common shift of one component moves the log-likelihood by **4e-9 in relative terms** (boundary effects only) — but a **0.1-pixel** one costs **7.3 nats**, because the model shifts by linear interpolation and a fractional shift blurs as well as moves. Left alone, therefore, a table's absolute level would be pinned by *interpolation error*: a number that looks like a systemic velocity, moves when the grid is resampled, and means nothing. albireo removes it. **The centering is done in pixel space, and that is not a detail.** With the relativistic mapping xi = artanh(v/c) — already chosen in M1 because shifts then compose and invert exactly — a constant pixel offset is *relativistic velocity addition*, not ordinary addition. Centering the pixel shifts makes the likelihood exactly invariant (measured: dlogL = 0.000e+00 at 5, 50 and 200 km/s on either component); centering the velocities instead leaves a residual four to six orders of magnitude larger, and the test pins the difference rather than asserting the choice. `inference.relative_velocities` maps the centered shifts back to km/s through the exact inverse, so the reported table is each component's velocity relative to its own mean epoch, with the variation — and therefore each semi-amplitude, and the Wilson slope, which is a slope and so immune to both zero points — untouched. **The uncertainty needed the same projection, and this is the trap worth recording.** Reading the Laplace covariance's diagonal returns **the prior and nothing else**: with Normal(0, 120) over 10 epochs every raw marginal sigma came out at 37.95 = 120/sqrt(10), identical to four digits across both components and all epochs, against an honest per-epoch error of **0.059 km/s** — a factor of 640, and insensitive to the data, so it would look equally convincing on a useless dataset. `relative_velocity_errors` projects each component's mean out of the covariance; the projected block then has **exactly n_stellar zero eigenvalues**, which is the identifiability claim confirmed numerically rather than argued. Posterior samples need none of this — the numpyro model records `velocity_rel` as a deterministic — and that route is what the docs steer to. **Measured.** 10 epochs, SNR 200, 400 model pixels at dv = 6 km/s, K = (30, 55), ell = (0.6, 0.4). Warm-started from a Keplerian **30% wrong in both semi-amplitudes**, the fit lands at per-epoch RV rms of **0.098 / 0.066 km/s** — 1/60th of a model pixel — recovers the Wilson slope to **0.4%** (-1.8255 against -1.8333 = -K2/K1), and threads the generating Keplerian at max residual 0.164 km/s (2.8 sigma) where a period wrong by 0.5% gives 2.98 km/s (50 sigma). Starts at the truth, at 10% error, at 30% error and at truth-plus-15-km/s-noise all reach the same optimum to four decimals. **The failure mode is real and is left visible rather than papered over**: from a cold start with every epoch at one velocity the components are indistinguishable and the fit lands at rms 12.9 / 28.7 km/s with a slope of the wrong sign — but at a potential **122,000 nats worse**, so it cannot be mistaken for a fit. The mode is documented as needing a warm start, which is also how it is meant to be used: fit a Keplerian, then free the velocities and ask whether the Keplerian still threads them (`inference.keplerian_residuals`, which centers both tables the same way and differences them in pixel space so the two arbitrary zero points cancel exactly) | benchmarks.md "D42"; math.md §7.6; `tests/test_velocity_table.py`; roadmap.md Tier 2 item 3 |
| D43 | Air vs vacuum wavelengths become a declared, validated property | `EpochData.medium` ('air' / 'vacuum' / None) plus `grids.air_to_vacuum` / `vacuum_to_air`, and a `Dataset` guard that **refuses a mixture** instead of picking one. Pulled forward out of roadmap item 7 because item 4's archive loader is what forced it: ESO Phase 3 declares the scale per file in `TUCD1` ('em.wl;obs.atmos' = air, bare 'em.wl' = vacuum), and the six instruments the loader targets are genuinely split — FEROS, HARPS, UVES and GIRAFFE deliver air, ESPRESSO and XQ-100 vacuum. A loader reading both without a field to record which would have been silently wrong. **The size of the error is the argument.** In Angstrom the offset grows with wavelength (0.87 A at 3000, 2.74 A at 10000), but as a *velocity* it is nearly constant at **83 km/s** — 87.4 at 3000 A, 82.8 at Halpha, 82.2 at 10000 A. That is the same order as the semi-amplitudes albireo exists to measure, it is systematic rather than random, and it does not average out over epochs. **None means undeclared, and is not a synonym for air.** Every epoch built before this field existed means None, so it stays legal and the default is backward compatible — but a Dataset mixing None with 'air' raises just as loudly as one mixing 'air' with 'vacuum', because 'unknown' is not a value that can be checked against anything. The alternative, defaulting to 'air' because most optical spectrographs deliver it, was rejected: a silently wrong default is exactly the failure mode the field exists to remove, and it would have made every pre-existing epoch assert something nobody had checked. **The conversion is the IAU-adopted Edlen (1966) / Birch & Downs (1994) refractivity evaluated at the *vacuum* wavenumber**, which is the convention Morton (2000) tabulates and therefore what published air line lists agree with. Inverting it is not closed-form, so `air_to_vacuum` iterates a fixed point twice — the refractivity moves by ~1e-8 over the 0.03% the wavelength does, so one pass is already good to ~1e-11 A and the round trip closes to float64 (measured: max error exactly 0.0 over 3000-10000 A). Tested against an independent plain-NumPy transcription of the published coefficients rather than against remembered line pairs — those come from different sources with different conventions and are not self-consistent to the milli-Angstrom, and a first draft of the test failed at Halpha for exactly that reason (the code was right; the remembered 6562.819 was not, the value is 6562.80) | `tests/test_wavelength_medium.py`; roadmap.md Tier 2 items 4 and 7 |

---

## 3. Data model

The core takes arrays; instrument I/O is out of scope (one thin loader utility only).

```
EpochData:                      # one observed spectrum
    wave        (N_j,) float64  # native wavelength grid, Å (vacuum or air — declared, not guessed)
    flux        (N_j,) float64  # continuum-normalized
    ivar        (N_j,) float64  # inverse variance; 0 = masked
    mask        (N_j,) bool     # optional convenience; folded into ivar
    bjd         float           # BJD_TDB mid-exposure
    v_bary      float           # barycentric correction velocity [km/s]
    instrument  str             # key into the LSF/response tables

Dataset: list[EpochData] + frame flag ("topocentric" | "barycentric"), validation, summary stats
```

Internally the dataset is packed into padded/ragged JAX arrays batched by instrument. Chip
gaps, cosmics, interstellar lines are all just `ivar = 0` — no special cases anywhere
downstream (weights/masks are honored in every operator and every solve).

## 4. Model & inference architecture

One equation summary (derivations in math.md §1–3): per epoch,
`m_j = diag(r_j) · R_j · [1 + Σ_i ℓ_ij · B_j · T(δ_ij) · d_i]`, i.e. shift → LSF-convolve →
rebin-to-native → response, linear in the stacked deviation spectra `d`. Marginalize `d`
analytically → sample θ with NUTS → recover `p(d | y)` as a mixture of conditional Gaussians
over posterior θ draws.

**Parameter split.**
- Linear (marginalized): component deviation spectra `d_i` — ~10⁵–10⁶ numbers we never sample.
- Nonlinear θ (sampled): orbit (~5–8), light fractions (0–N_c·J), LSF widths (per instrument),
  response coefficients (J·(order+1)), jitters (≤J), prior hypers (≤2·N_c). Typically 20–250.

**Modes.** (all share one code path — a mode is a choice of velocity law + what's fixed)
1. SB2/SB3 Keplerian joint inference (flagship).
2. Free per-epoch velocities (diagnostic).
3. Fixed-orbit linear solve (M2 milestone; also the "give me spectra now" utility).
4. SB1 + faint companion K₂ scan → Δlogℒ detection maps + recovered secondary spectrum
   (math.md §6), vmapped over the grid.

**Outputs** (`FitResult` / `PosteriorResult`): θ posterior (arviz-compatible), conditional
spectra draws + pointwise bands + banded covariance info, whitened residual diagnostics,
degeneracy report (low-k variance spectrum, response–spectrum covariances), sensitivity
forecast utility.

## 5. Degeneracy policy

Derivations in math.md §5. Policy: **never silently regularize away a real degeneracy** —
make it proper with an explicit prior scale, report it, and where only external information
helps, require the user to choose.

| Degeneracy | Policy |
|---|---|
| Low-frequency mode exchange ("undulations") | proper priors with documented scale; posterior covariance reports inflation; `sensitivity_forecast()` for observing strategy |
| Light ratio ↔ line depth | **mandatory explicit choice**: `Fixed` / `Free(prior=...)` / `PerEpoch(eclipse model)`; docs explain what breaks it |
| γ ↔ common shift | γ ≡ 0 default; post-hoc template measurement; free-γ requires informative prior |
| Response ↔ broad features | low default order; covariance reported |

## 6. API sketch (target user code)

```python
import albireo as ab

ds = ab.Dataset(
    [ab.EpochData(wave=w, flux=f, ivar=iv, bjd=t, v_bary=vb, instrument="HERMES"), ...],
    frame="topocentric",
)

dis = ab.Disentangler(
    dataset=ds,
    grid=ab.LogGrid.from_wavelength_range(4000.0, 6800.0, dv_kms=1.0),
    components=[ab.Star("A"), ab.Star("B"), ab.Telluric()],
    orbit=ab.Keplerian(
        period=ab.Normal(11.55, 0.01),  # priors are first-class
        t_peri=...,
        ecc=...,
        omega=...,
        k1=...,
        k2=...,
    ),
    light_ratio=ab.FixedLight([0.62, 0.38]),  # explicit, or Free(prior=...) / PerEpoch(...)
    lsf={"HERMES": ab.GaussianLSF(sigma_v_kms=2.4)},
    response=ab.Chebyshev(order=2),
    spectral_prior=ab.Smoothness(tau="ml2", eta=1e-4),
)

quick = dis.fit_map()  # MAP + conditional spectra, seconds
post = dis.sample(num_warmup=1000, num_samples=1000, seed=0)  # NUTS over θ
spec = dis.conditional_spectra(post)  # draws, bands, covariance
rep = dis.degeneracy_report(post)

# diagnostic + detection modes
free = dis.replace(orbit=ab.FreeVelocities()).fit_map()
scan = ab.K2Scan(dis, k2_grid=np.linspace(1, 150, 300)).run()  # SB1 workflow
```

## 7. Package layout & engineering

```
src/albireo/
    __init__.py        # x64 enforcement, public API re-exports
    grids.py           # LogGrid, doppler log-shift mapping          [M0 — this session]
    operators.py       # shift/interp/rebin linear ops + adjoints    [M0 — this session]
    kepler.py          # differentiable Kepler solver, velocity laws [M1]
    simulate.py        # simulator = test harness                    [M1]
    forward.py         # epoch forward model assembly                [M2]
    solver.py          # banded/block-tridiag Cholesky, logdet, solves, sampling [M2]
    likelihood.py      # marginal likelihood (strategies A/B/C)      [M2–M3]
    priors.py          # spectral priors, parameter priors           [M2]
    inference.py       # MAP (optax), NUTS (numpyro), results        [M3]
    scan.py            # K2-scan mode                                [M4]
    data.py            # EpochData/Dataset, validation, thin loader  [M1]
    preprocess.py      # continuum, ivar estimation, region/masking, grid sharing [D30]
    io.py              # FITS -> RawSpectrum -> EpochData/Dataset (astropy) [D30]
tests/                 # mirrors modules; simulator-driven closed-loop tests
docs/                  # mkdocs-material; design.md, math.md, tutorials (executable, in CI)
```

Standards (per brief): JAX with x64; CPU-first correctness, GPU as accelerator; type hints +
docstrings on all public API; pytest with closed-loop tolerance tests, adjoint tests for every
linear operator, gradcheck vs. finite differences; GitHub Actions CI on ubuntu + windows;
pre-commit (ruff, ruff-format); BSD-3; SemVer. Performance work is benchmark-gated: no
optimization without a recorded baseline in `docs/benchmarks.md` (started at M2).

Test philosophy: the simulator (M1) is the oracle — every inference feature ships with a
closed-loop test against known injected truth, and calibration claims (M3+) are backed by
SBC/coverage runs, not asserted.

## 8. Milestones

| | Deliverable | Acceptance gate |
|---|---|---|
| **M0** | scaffolding, design.md, math.md, grid + shift/resample operators with adjoint/gradcheck/flux tests | **this session — stop for review** |
| M1 | simulator (templates, orbit, LSF, noise, tellurics, response) | simulated datasets reproduce all advertised pathologies (gaps, mixed instruments) |
| M2 | fixed-orbit linear solver + uncertainties | <1% RMS recovery in line regions on SNR-100/30-epoch sims; whitened residuals ~N(0,1); marginal likelihood matches dense brute force on small problems; nullspace analysis documented |
| M3 | joint NUTS inference + MAP pipeline | SBC/coverage pass on injections; K₁,K₂ to <1% at design SNR |
| M4 | tellurics, SB3, per-epoch light, multi-instrument LSF, K₂-scan | closed-loop tests per feature |
| M5 | benchmarks vs. fd3, one real published SB2 end-to-end, docs polish, JOSS checklist | definition of done: 2×10⁵ px / 50 epochs samples in minutes on one GPU; tutorials run in CI. Candidate headline case: an LB-1/HR 6819-type system, where the posterior exposes the hand-set-light-ratio systematic that drove the published debate |

## 9. Non-goals (v1) and v2 seams

Not in v1: GUI; Fourier-space methods; time-variable component spectra (pulsations/spots);
emission-line-specific models; apsidal motion; exhaustive instrument I/O.

Designed seams for v2: components carry their own velocity-law and (future) time-dependence
interface — a pulsating component would subclass the component protocol without touching the
solver; LSF is an operator slot (Gaussian → tabulated is a swap); the Keplerian core is a
standalone module shared with a future differentiable eclipsing-binary package for joint
light-curve + RV + disentangling fits; strategies A/B share one solver interface so new
structured solvers slot in.
