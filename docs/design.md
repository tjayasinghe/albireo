# albireo — design document (v1)

**Status:** M0 draft for review. Companion document: [math.md](math.md); the equations are
there, this document records decisions.

albireo is an open-source Python package for spectral disentangling of double- and
multiple-lined spectroscopic binaries: given a time series of composite spectra, it infers the
orbit and the individual component spectra jointly, with uncertainties on both. The name comes
from Albireo, the gold-and-blue double star in Cygnus. Intended users: massive-star binary
surveys, dormant black-hole and neutron-star companion searches, benchmark eclipsing binaries,
and multi-epoch survey spectroscopy (APOGEE/SDSS-V, WEAVE, 4MOST). Purpose: an openly licensed,
scriptable alternative to fd3 and KOREL, described by a methods paper (A&A/ApJS) and a JOSS
paper, installable with pip and shipped with tutorials.

---

## 1. Prior art

| Method / code | Domain and approach | Strengths | Failure modes | Adopted / avoided |
|---|---|---|---|---|
| **Simon & Sturm 1994** (A&A 281, 286) | wavelength space; large sparse linear system, SVD, wrapped in orbit optimization | formulates disentangling as a linear inverse problem; natural weights and masks; arbitrary per-epoch RVs | SVD cost and memory grow rapidly with N; ill-conditioned low-frequency nullspace, no regularization; no uncertainty quantification | **Adopt** the linear-algebra formulation as the conceptual ancestor; replace the SVD with structured/banded solvers and priors |
| **KOREL** (Hadrava 1995, A&AS 114, 393; 2004 guide; VO service, Škoda+2012) | Fourier space; simultaneous orbit and spectra; line-strength variability | most feature-rich incumbent; decades of use | Fourier low-frequency degeneracy; closed-source FORTRAN behind a registration-gated VO service (KOREL11b, 2011), so it is neither scriptable nor reproducible offline; formal errors long criticized; no uncertainty quantification on spectra | **Avoid** Fourier space entirely; explain undulations as nullspace (math.md §5.1) |
| **fd3 / FDBinary** (Ilijić+2004, ASPC 318, 111; ascl:1705.012) | Fourier space, C + GSL, CLI | fast, template-free, triple-capable (hierarchical), scriptable, heavily cited | requires equidistant common log-λ sampling; Keplerian-only RVs; low-frequency "bias progression" (Hensberge+2008); no uncertainty quantification anywhere; page last updated 2014, no license statement | **Avoid**; fd3 is the benchmark target for accuracy and wall-time (M5) |
| **CRES** (Ilijić) / **Spectangular** (Sablowski & Weber 2017, A&A 597, A125; 2019, A&A 623, A31; C++/Qt, Apache-2.0) | wavelength space; SVD + global optimization of orbit or per-epoch RVs | wavelength-space flexibility (weights, masks); global optimizer; handles line-strength variability | CRES abandoned (2006), takes RVs as input; Spectangular GUI-centric, hard to batch, small user community, point estimates only | direction validated; albireo adds scalability and Bayesian inference |
| **Shift-and-add grid disentangling** (González & Levato 2006; Shenar et al. 2020, A&A 639, L6 & 641, A162, LB-1/HR 6819; code: TomerShenar/Disentangling_Shift_And_Add) | iterative shift-and-add over a (K₁,K₂) χ² grid | simple, robust at low SNR, de facto standard in the massive-star and BH-candidate community; χ² maps show the K degeneracies | grid search scales exponentially in dimension; slow or stalling iteration acts as an uncontrolled implicit regularizer; light ratios are set by hand, which is the crux of the LB-1/HR 6819 debate; no posterior; the repository carries no license, so it cannot be vendored | **Adopt** the use cases and diagnostics (K₂ scans, detection maps) as first-class features; replace the machinery |
| **PSOAP** (Czekala et al. 2017, ApJ 840, 49; MIT, dormant since 2017-12) | GP joint inference in wavelength space; component spectra marginalized analytically | joint Bayesian posterior over orbit and spectra; the closest ancestor of albireo's design | dense GP covariances over epochs × pixels give cubic cost and quadratic memory, forcing small chunks; NumPy/Cython, no autodiff or GPU; unmaintained | **Adopt** the approach; **avoid** dense kernels in favour of banded precision priors and structured solves (math.md §2, §4) |
| **Seeburger et al. 2024** (MNRAS 530, 1935, "autonomous disentangling for surveys") | wavelength space; second-derivative Tikhonov + sparse LSMR solve; Nelder–Mead over RVs/q | closest methodological competitor; survey-oriented; validated on the Unicorn/Giraffe | not Bayesian, no posteriors, no GPU or autodiff; no public code as of 2026-08 | their curvature penalty is the deterministic limit of the τ·D₂ᵀD₂ prior used here, which additionally yields a posterior; benchmark against it |
| **DOLBY** (Sairam et al. 2024, BEBOP VI, MNRAS) | sum of two Doppler-shifted GPs (wavelength or CCF space) | the only active GP/Bayesian effort since PSOAP | aimed at RV precision for circumbinary planets, not spectrum recovery; code availability unverified | adjacent niche; cite and compare |
| **BiSpeD** (González, Martínez & Alejo 2024, A&A 690, A124; Martínez & González 2025, A&A 694, A32; MIT, actively maintained) | SB1: grid over q, subtract primary, cross-correlate residuals against templates | covers the low-q (≲0.5) faint-companion regime; pip-friendly | not a general SB2 joint solver; template-grid dependent; grid search, no posterior | overlapping niche, covered by the SB1 K₂-scan mode (template-free, marginalized) |

Also surveyed: QER20 (Quintero & Eenens 2024, MNRAS 532, 2604; code unverified) and
single-exposure neural-network parameter recovery (Binnenfeld et al. 2025), which bypasses
disentangling. A targeted search in 2026-08 found no JAX, GPU, differentiable or Bayesian
disentangling code.

The gap these codes leave: none offers (a) joint Bayesian inference of orbit and spectra with
uncertainties on both, the common workaround being a bootstrap over epochs for orbit errors and
no uncertainty at all for the spectra, (b) at survey scale, (c) with current engineering
practice (GPU, autodiff, tests, docs, pip). albireo targets all three, (a) first.

The method (math.md §3): conditional on the nonlinear parameters θ (orbit, light fractions,
LSF, response, hyperparameters), the component spectra enter linearly. With Gaussian noise and
Gaussian smoothness priors they marginalize analytically, so NUTS samples only ~10–200
nonlinear parameters regardless of spectrum size; the spectra and their full covariance are
recovered afterwards as a conditional Gaussian. This is the linear-marginalization technique
used by celerite and starry, applied to disentangling, and it is the difference between this
package and iterative shift-and-add.

---

## 2. Decisions & recorded defaults

No blocking questions were open after the project brief. The following defaults were assumed
and form the ledger for review; each is cheap to change now and expensive later.

| # | Decision | Default | Rationale / alternative |
|---|---|---|---|
| D1 | Model grid | uniform in ln λ; pixel `dv` = ~half the finest instrument pixel | a Doppler shift becomes a translation; oversampling controls the interpolation error |
| D2 | Doppler mapping | relativistic, ξ(v) = artanh(v/c) | antisymmetric, so composition and inversion are exact; the classical form is off by ~0.6 km/s at 600 km/s and is available as an option |
| D3 | Shift interpolation | linear (2-tap), zero-fill, on *deviation* spectra d = s − 1 | sparse, exact adjoint; cubic 4-tap behind a flag later (math.md §1.1) |
| D4 | Data resampling | data are never resampled; the model is projected to each epoch's native grid (pixel-integral rebin operator) | keeps the noise diagonal; mixed-instrument datasets need no extra machinery |
| D5 | Frames | data frame declared per dataset (`topocentric` default); barycentric correction composed inside the model; tellurics = static component in topocentric frame | one convention, one function, tested |
| D6 | Tellurics | optional additive linear component (first-order approx of multiplicative transmission) | keeps linearity; error ~d★·d_tell, and deep tellurics are masked; exact treatment is v2 |
| D7 | Response | multiplicative per-epoch Chebyshev, order 2 default, coefficients in θ (build-time constants through M4: they enter the weights and targets, not just the operator, so their θ-swap was deferred; built in D33, math.md §7.5) | absorbs normalization errors; low order to protect broad features |
| D8 | LSF | per-instrument Gaussian σ_v (constant-R); tabulated LSF is v2 (banded matrix, no structural change; built in D37, where per-anchor widths are interpolated into a per-pixel profile bank, `operators.convolve_varying`, and the operator accepts arbitrary asymmetric banks) | mixed-resolution datasets supported from day one |
| D9 | Spectra priors | banded-precision smoothness (τ D₂ᵀD₂ + η I) per component; τ, η fixed/ML-II/sampled | scalable (no dense kernels); the nullspace is made proper explicitly |
| D10 | Marginal-likelihood engine | Strategy A: block-tridiagonal (banded) Cholesky, `lax.scan` + `vmap` over chunks/systems | ~10¹¹ flops at the design target, so ~0.1 s/eval on GPU; B (CG+SLQ) for benchmarks; C (frozen logdet) quick-look only (math.md §4) |
| D11 | Sampler | numpyro NUTS via `numpyro.factor` for the marginal likelihood (keeps priors/transforms/summaries); blackjax kept compatible | mature, well-documented, custom log-density path confirmed in numpyro docs |
| D12 | MAP | optax/jaxopt on the same objective; `fit_map()` is the quick-look path | |
| D13 | Light ratio | no default: the API requires an explicit `Fixed` / `Free(prior)` / `PerEpoch` | exact degeneracy with line depths (math.md §5.2); a default would assert a quantity the data cannot constrain |
| D14 | Systemic velocity | γ ≡ 0 internally (spectra in systemic frame); free-γ only with informative prior | γ is unidentified by disentangling (math.md §5.3) |
| D15 | Noise model | diagonal ivar, mask = zero weight, optional per-epoch jitter factor (built in D31) | |
| D16 | Precision | float64 mandatory; enabled at `import albireo` (opt-out env var `ALBIREO_DISABLE_X64` for experiments) | adjoint and logdet tests require it, as does spectrum recovery |
| D17 | Stack | Python ≥3.12 (deviation from the brief's ≥3.11: current jaxlib 0.11.x ships no cp311 wheels), JAX, numpyro, optax; hatchling; src layout; ruff; pytest; GH Actions (ubuntu+windows, 3.12/3.13); mkdocs-material; BSD-3 | verified against PyPI 2026-08-11 |
| D18 | Package name | `albireo`, verified free on PyPI (2026-08-11); register early | fallback `albireo-spectra` if upload is admin-blocked |
| D19 | Versioning | SemVer, `0.1.0.dev0` now; public API = documented API | |
| D20 | Orbit parameterization | sampled sites (M3): `period`, `t_conj`, `secosw = √e cos ω`, `sesinw = √e sin ω`, `k` (vector); user-chosen numpyro priors supply the scale transforms (e.g. LogNormal for P, K); disk constraint e < 1 via −∞ factor with e clipped at 0.95 (the Kepler solver's verified range); γ ≡ 0 (D14) | the √e pair is smooth through e = 0 and uniform on the disk, so e ~ U(0,1); t_conj is well-defined for circular orbits (math.md §7.2) |
| D21 | Static solver bandwidth | user declares `v_rel_max_kms`; bandwidth bound fixed at build time (jit-static); the numpyro model adds a bandwidth guard (−∞ when realized shifts exceed the budget) | probing with too small a band is silently wrong; the guard makes wide priors safe by construction (math.md §7.1) |
| D22 | Spectral hyperparameters (τ, η) | empirical Bayes default: MAP jointly over (θ, log τ, log η) = ML-II (spectra already marginalized); NUTS runs with hypers conditioned at ML-II values; full sampling available by putting them in the priors dict | the prior scale sets the answer for sub-LSF modes (math.md §5.1, §7.3), so it is estimated rather than assumed; the marginal's log det Λp Occam term makes ML-II well-posed |
| D23 | SB3 = hierarchical nested Keplerians | optional θ sites `period_out`, `t_conj_out`, `secosw_out`, `sesinw_out`, `k_out = (K_AB, K_C)` (all five or none); inner components ride the outer center-of-mass RV, tertiary opposes with ω_out + π; outer light-time effect neglected (v2 seam) | matches fd3's triple topology; the tertiary is one more linear component (math.md §7.5) |
| D24 | Light fractions in θ | optional `light` site: `(n_stellar,)` constant or `(n_epochs, n_stellar)` per-epoch, rows on the simplex (Dirichlet priors); swaps into the static graph like the shifts | ℓ enters A(θ) linearly; the D13 eclipse breaker becomes inferred, closing the loop on math.md §5.2 |
| D25 | LSF widths in θ | optional `lsf_sigma` site (one per instrument, model-group order); construction-time widths fix the kernel radii and act as strict upper bounds (−∞ guard above); absolute widths require a reference-instrument anchor (tight prior), since only relative widths are data-identified | template-free models cannot separate the LSF from the intrinsic line width (measured: all-free ML-II inflates widths by tens of per cent, orbit unaffected); same anchor policy as D13 |
| D26 | K₂-scan mode | `scan.k2_scan`: null = single-component model (ℓ₁ = 1, same primary prior); companion ℓ₂ explicit (D13); D(K₂) calibrated by injections, no χ² claim, since the Occam term makes D < 0 under the null (verified) | math.md §6; the marginalized matched filter replaces BiSpeD and shift-and-add |
| D27 | Scale architecture (M5) | `Problem` is a registered pytree passed as a jit argument (closure-captured arrays trigger multi-GB XLA constant folding at scale); probing runs as a sequential scan over probe batches with per-block gather readout, because unrolled batches let XLA overlap live ranges (~80 GB temporary at 2×10⁵ px, measured) and un-remat scans store every batch for the backward pass (~470 GB); batch size and backward rematerialization are size-adaptive (large batch and no remat below a ~64 MB probe-output footprint, where remat doubled gate-scale NUTS wall time for no benefit; small batch and remat above it); the prior factor gets its own small block size | benchmarks.md M5; `probe_chunk`/`remat` stay exposed as the CPU/GPU tuning knobs |
| D28 | Direct band assembly + closed-form gradient (M5 speedup pass) | the posterior precision is assembled per epoch from its analytic band structure: each epoch's (i,j) block is ℓᵢℓⱼ·T(δᵢ)ᵀ·(KᵀRᵀW′RK)·T(δⱼ), a band of width ~2·(rebin support + 2·kernel radius) at a velocity-set offset, built from static rebin pair tables (`operators.rebin_pair_tables`, stored on `EpochGroup`), a vmapped kernel-sandwich pre-pass, and a `lax.scan` accumulating tent-weighted `dynamic_slice` translations into a band tensor (`assembly.band_block_tridiagonal`; math.md §4.5). This replaces 2p+1 global probe matvecs with O(band width) work per epoch (~8× per evaluation, measured); comb probing is retained as the reference path (`assembly="probe"`) and as the `validate=True` oracle. The solve stage carries a custom VJP (`likelihood._solve_stage`): cotangents come from the block-Takahashi banded selected inverse (`solver.selected_inverse_blocks`) and d̂-outer-products, removing reverse-mode differentiation through the Cholesky/solve scans; gradients flow through `log_likelihood`, `quad`, and `d_hat` but not through the returned Cholesky factor (a documented contract). Row translations use clip-safe `dynamic_slice` on double-padded arrays, which gives exact zero-fill for any shift and makes the reverse pass a contiguous copy rather than the scatter a gather-based translation would require. Second derivatives: reverse-over-reverse only (the forward rule recomputes its primal inline so `jacrev(jacrev(...))` stays exact; forward mode is rejected by `custom_vjp`, and `jax.hessian`'s forward-over-reverse was independently measured asymmetric on this stack even for plain autodiff, so `laplace_inverse_mass` switched to reverse-over-reverse, fixing a defect present since M3). Remaining levers on the ledger, not implemented: analytic VJP through the assembly itself, custom-VJP band→block packing, associative-scan (parallel-prefix) Cholesky for GPU chain latency, opt-in mixed precision, fusing the Takahashi sweep into cotangent assembly (the design-target gradient is memory-bound at 32 GB). That list is superseded by D29 and D49: D29 performed the Takahashi fusion; D49 measured the band→block packing at 0.20 s of a 10 s gradient and left it unimplemented, found the item worth 3.5 s (the band accumulate's transpose, which is absent from this list), and measured a blocked Cholesky at 1.36× | benchmarks.md M5 speedup pass; band-vs-probe and VJP-vs-autodiff equality are regression-tested in tests/test_assembly.py |
| D29 | Memory pass + boundary correctness (post-D28) | The design-target gradient measured 34.0 GB against 32 GB of RAM, so it did not fit. Four reductions, all exact: (1) the Takahashi sweep now emits the cotangent blocks directly (`solver.selected_inverse_cotangent`), so the selected inverse is never materialized; (2) the velocity-independent `G` pre-pass is batched over epochs (`epoch_chunk`), since `vmap` batches every intermediate of the chain and not only the result, which left the un-batched pre-pass at ~9 GB; batching costs one extra `G` pass in the backward and is therefore applied only above a ~1 GB threshold, leaving small and gate-scale problems on the fast path; (3) the prior determinant uses a scalar pentadiagonal recursion (`assembly.prior_logdet`) instead of a block-tridiagonal factorization of a bandwidth-2 matrix; (4) `_pack_band`'s gather indices are hoisted out of the `lax.map` body (they never depended on the block counter) and the body is rematerialized, so reverse mode stops stacking (B,B) index and mask arrays over all K blocks. Result at the design target: eval 20.6 → 11.1 GB, gradient 34.0 → 18.2 GB, and, because the gradient had been thrashing against RAM, eval 26.0 → 22.2 s and gradient 111.3 → 87.4 s with it. (Mid-ladder gradients pay 5–23% for the batched pre-pass's extra backward `G` pass, where memory was not the binding constraint; `epoch_chunk` trades it back.) Correctness was fixed in the same pass: the LSF convolution wrote `G` band entries at column indices outside the model grid and the T-sandwich read them, because only the sandwich's row index was zero-filled. The trigger is a data-coverage margin (in model pixels) below the kernel radius, which includes choosing a model grid narrower than the observed range to fit a sub-region, and is reachable at a fixed grid by fitting a wider LSF. At zero margin the assembled precision was wrong by 6.8e-2 relative and asymmetric by the same amount (probing is symmetric), worth −57 nats of log-likelihood. Every fixture left a margin exceeding the kernel radius, so the weights vanished where the defect lived. `G`'s out-of-grid columns are now masked at the source. Also: the Cholesky factor left `_solve_stage`'s outputs, and a cotangent on it cannot be honoured by the closed-form rule, so `spectra_std`/`draw_spectra` gradients were silently zero; `MarginalResult` now stores the precision and rebuilds the factor outside the custom boundary (one extra Cholesky, paid only by callers that want it). `build_problem` warns when a few anomalously wide native pixels set `row_support`, that is, deleted telluric or order gaps rather than masked ones, which inflates the solver bandwidth quadratically | benchmarks.md "D29 memory pass"; the boundary case is now a fixture (`edge_covered`) compared entrywise against probing, since the log-determinant and solve it previously guarded average the defect away |
| D30 | Real-data ingestion path (first observed dataset: HR 6819) | Two new modules and four fixes, driven by putting 51 archival ESO Phase-3 FEROS spectra through the stack. `albireo.preprocess` (pure NumPy, like `data.py`) provides `fit_continuum` / `normalize`, an asymmetric-envelope-then-asymmetric-sigma-clip penalized fit of `log(flux)` on a knot grid. The fit is done in the log because a continuum is multiplicative (the measured FEROS response falls 20× over 3850–4750 Å, which a curvature penalty in linear space cannot track: normalized flux came out 30% wrong) and because a per-pixel Whittaker smoother needs `lam ~ (L_px/2π)^4 ~ 1e12` at echelle smoothing lengths, at which the weight term is lost to rounding and the SPD factorization fails outright; knots at 8 per smoothing length hold `lam ~ 2.6`. `estimate_ivar` applies DER_SNR (Stoehr et al. 2008, ESO's own recipe) in wavelength bins, fitted to `sigma^2 = s^2/continuum`, because the ESO FEROS `ERR` column is entirely NaN. `select_region` / `mask_ranges` / `mask_tellurics` / `mask_spikes` mask by setting `ivar = 0`, never by deletion (D29's row-support trap). `share_wavelength_grid` relabels per-exposure grids onto one grid by index, never by value (a `searchsorted` at a window edge slips a whole 1.4 km/s pixel), asserting that the residual is within tolerance; it is a relabelling, not a resampling, so D4 stands. `albireo.io` (optional `astropy`, `[io]` extra) provides `read_spectrum` for ESO Phase-3 binary tables and WCS image spectra, resolving the frame from `SPECSYS`, `v_bary` from the pipeline's own keyword (`ESO DRS BARYCORR` and equivalents, since the pipeline's value defines the delivered frame; astropy is used only as a fallback, agreement measured at 0.017 km/s), and BJD_TDB at mid-exposure from the extension-header `TMID`; every assumption it has to make emits a warning naming it. `LogGrid.covering` sizes the model-grid margin as shift + kernel radius + slack. Fixes found by the data, all with regression tests: (1) `build_problem` formed `z = flux - r·base` unmasked, so a non-finite flux at a zero-weight pixel, which `data.py` documents as legal and which `normalize` produces at a collapsed continuum, took the whole marginal likelihood to `nan` via `0 * nan`; (2) epochs of one instrument had to share a bit-identical `wave` array or `build_problem` raised, but a pipeline that shifts before resampling gives one grid per exposure (51 spectra, 28 grids), so grouping is now by (instrument, grid) with the instrument key still keying the LSF, and `_epoch_chunk_default` divides its byte budget by the group count; (3) `prior_logdet`'s Cholesky pivot returned `nan` below `eta/tau ~ 1e-13`, reachable by unbounded ML-II, and is now floored; (4) the row-support warning seeded its minimum with `int64` max and never restricted to rows the operator touches, so a region-selected epoch produced a warning quoting nonsense. `run_map` gained a `callback` (it was silent for hours, and its absolute `tol` is unreachable at survey pixel counts). Recorded and not fixed: light fractions remain an input for a non-eclipsing system (D13; the K-band GRAVITY ratio does not transfer to 4400 Å), there is still no jitter site (D15), so estimated inverse variances must be checked against `data_residual_zscores` (built afterwards, see D31), and the numpyro path captures `Problem` as a closure constant rather than a jit argument, so it pays the XLA constant folding D27 avoids for `MarginalOrbitModel.marginal` (fixed in D32) | benchmarks.md "HR 6819"; `tests/test_preprocess.py`, `tests/test_io.py`, `examples/03_hr6819_real_data.py` |
| D31 | Jitter site built (closes D15) | `EpochGroup` carries a per-epoch factor α_j. Every consumer of the weights reads `effective_w = w/α²` and none reads `w`, so the inflation reaches the normal matrix, the right-hand side and the `Σ log w` term together, which is what makes α identifiable rather than a free knob. It is swapped in by `forward.with_jitter` (scalar or per-epoch, traced-safe, replacing rather than compounding, since the raw `w` is kept) and exposed as the optional θ site `log_jitter`. What the marginal estimates: in the data-dominated limit `−½ log det(Λ + AᵀWA)` contributes `+p_eff·log α` against the weight term's `−N·log α`, so profiling gives `α² = χ²/(N − p_eff)` with `p_eff = tr[(Λ + AᵀWA)⁻¹AᵀWA]`, the effective-dof-corrected variance estimate rather than `χ²/N`. The naive route (whiten the residuals, read off their standard deviation, rescale ivar) is low by `√(1 − p_eff/N)`, 4.6% in the weak-prior fixture. The size of the correction is a property of the run, and `p_eff` is not `n_comp · n_pix`: on HR 6819 an oversampled grid plus a fitted smoothness prior gave `p_eff ≈ 2900` against 19,876 model pixels, about the resolution-element count, so the correction there was 0.4%. Inverting the two estimators, `p_eff = N[1 − (sd/α̂)²]`, gives an otherwise awkward diagnostic of how much of the spectrum the data constrain. Regression-tested by injecting a known ivar scale error and recovering it to 0.8%. Limits, documented at the function: a diagonal inflation cannot represent a residual correlated across pixels, so a jitter fitted against systematics (continuum, LSF mismatch, line-profile variability) reports a wider but still biased orbit; `data_residual_zscores` now whitens by the assumed weights, jitter included, so after fitting one it reads ≈1 by construction and the remaining diagnostic is the residual's shape rather than its scale. Measured on HR 6819, the result is a caution rather than an improvement: per-epoch α runs 1.11–3.61 (the worst exposure ends up with 1/13 of its nominal weight) and buys +19,763 nats over one shared α, the residuals whiten to sd 0.997 as expected, and the period moves by 174× the no-jitter formal error, away from the published value, because the noisiest exposures cluster in the first third of the baseline (`corr(α, phase) = −0.25`) and downweighting them removes the period leverage at one end. Both fits are optima under their own weights (cross-evaluated: +10,622 nats one way, +3,835 the other), so this is the noise model selecting the optimum rather than a defect. Window-to-window disagreement in period went from 5.6× to 13.1× the combined formal error. The standing recommendation gains a clause: quote the spread across independent windows and across defensible noise models | `tests/test_jitter.py`; math.md §3.2a; benchmarks.md "The jitter site" |
| D32 | numpyro path traces the problem (closes D30's recorded leftover) | `MarginalOrbitModel.model()` takes the base `Problem` as an optional model argument and advertises it via a `model_args` attribute on the returned closure; `run_map` / `laplace_inverse_mass` / `run_nuts` resolve the arguments (explicit > model attribute > none) and thread them through numpyro as traced jit arguments, using `initialize_model(dynamic_args=True, model_args=...)` for the MAP/Laplace potential and `MCMC(jit_model_args=True)` for the NUTS sample loop. This extends the D27 contract to every jitted entry point with no call-site changes. Measured on the m5 ladder (50 epochs, CPU, value+grad of the potential): compile 20.9 → 0.8 s at 31.7k px and 10.8 → 0.8 s at 74.3k px; the closure build folded 1.99 GiB of derived constants into the executable at 31.7k px (+1.35 GiB process peak at compile, XLA's slow-folding alarm firing on `f64[50,126936]` scatter-adds), while at 74.3k px XLA's folding guards declined the largest folds. The memory cost is therefore heuristic-gated across scales and versions, which argues for removing the exposure rather than relying on the guard (D27 measured ~80 GB from the same mechanism at the design target). Results are unchanged: potential and gradient agree to all printed digits, run_map paths agree to float tolerance, Laplace exactly. A model called with no argument (plain numpyro utilities, `log_density`) falls back to the closure, which is correct but not compile-safe at scale; `model_args=()` forces that path explicitly | benchmarks.md "D32"; `tests/test_inference.py` jaxpr-consts probe + path-agreement tests; `scripts/d32_model_args_bench.py` |
| D33 | Response θ-site (closes D7's deferral, the per-epoch continuum treatment) | `forward.with_response` plus the optional θ site `response` (`(n_coef,)` shared or `(n_epochs, n_coef)` per-epoch, laid out as the `light` site). The response enters the targets `z = y − r(R·1)` and the sandwich weights `w r²`, not just the operator, which is why D7 deferred it; but `R·1` is response-independent and is now stored per group, so `z_new = z_old + (r_old − r_new)·R·1` rebuilds the target exactly with no raw fluxes carried (re-masked, so D30's `0·nan` trap cannot resurface). `Σ log w` is untouched (the noise lives on the data), and the site replaces rather than compounds, like the jitter. The traced Clenshaw recursion matches `np.polynomial.chebyshev.chebval` operation for operation: the swap equals a fresh build with `r` bitwise equal, marginal rtol 1e-12. Identifiability, measured (§5's response row): the closed loop recovers the epoch-to-epoch coefficient differences to 2.0e-3 rms against an injected 3.4e-2 with K to 0.2%, while the epoch-shared mode lands at its zero-centered prior rather than at truth (c₀ error −0.069 against prior σ 0.05). Keep the order low, the priors tight, and read the common mode as a normalization convention. On HR 6819 the site exonerates the continuum: +4,100 / +3,926 nats (windows A/B) absorbed by per-mil coefficients (difference mode 5e-4 rms in both windows), and nothing else moves. Period +0.0004/−0.0002 d (0.4σ/0.2σ formal, against the jitter's 174σ relocation), K by <0.001 km/s, residual sd −0.4% (1.674→1.668, 1.401→1.394), window-to-window spread unchanged (ΔP 2.8σ→3.1σ, ΔK 10.5σ). The D30 suspect list loses "per-epoch continuum residuals"; the survivors are the pipeline resampling's pixel correlations, the Gaussian LSF stand-in, and disc variability, so the recorded next steps are a correlated-noise model and a wider window. Separately, window B's uniform-procedure baseline MAP sits 0.0093 d (6.6× the combined formal σ) from the D30-recorded window-B period, multimodality demonstration #3, and the reason the comparison only pairs fits sharing one procedure | benchmarks.md "D33"; `tests/test_response.py`; `scripts/hr6819_response_run.py`; math.md §7.5 |
| D34 | Correlated noise: per-epoch AR(1) chain (the top surviving D30 suspect, the pipeline's resampling) | `C = α²·D^(−1/2)·R_φ·D^(−1/2)` (math.md §1.4a): the correlation matrix has unit diagonal by construction, so supplied per-pixel variances are kept and the residual-sd diagnostic is provably blind to φ. The discriminator is the lag-1 autocorrelation of the whitened residuals (~φ under diagonal whitening, ~0 under the chain whitener). A subset of a Markov chain is Markov, so masked pixels are exact rather than approximated: each gap collapses to a single link with `ρ = φ^gap` (capped at build, `ar1_max_gap=4`, beyond which the chain restarts); the noise precision stays tridiagonal with a closed-form `log det` and an innovation whitener, and every consumer of the weights reads it (normal matrix, rhs, quad, `Σ log`-terms, `data_residual_zscores`), which is the D31 identifiability argument again. It is swapped in by `forward.with_ar1` (scalar or per-epoch, replacing like the jitter), θ site `ar1_phi`, behind an explicit `MarginalOrbitModel(ar1=True)` opt-in, because the chain couples across masked gaps and the solver bandwidth grows by a statically reserved `ar_bandwidth_extra` (D21's declared-bandwidth philosophy; 5–6 model px on HR 6819). Correlated problems are rejected by the D28 band assembly, which assumes diagonal weights, and auto-select the probe path at ~15× the D33 per-step cost measured on HR 6819; the tridiagonal band-sandwich extension is a recorded lever, to be built only if AR(1) earns a permanent place. Exactness against a dense reference sharing nothing with the closed forms (chain correlation matrix from link products, LAPACK-inverted): marginal rtol 1e-10 with gaps and jitter composed, φ=0 identical to the diagonal model at rtol 1e-12, finite-difference gradients including exactly at φ=0 (`jnp.power` has a nan gradient at zero base, so gap-1 links use φ directly). Closed loop: φ=0.45 and a 1.5× ivar overstatement injected together, recovered jointly with the orbit at φ̂ 0.4493, α̂ 1.4868, K to 0.31%. On HR 6819 the chain is the first noise model in the campaign to whiten both moments while leaving the orbit in place: φ̂ +0.801 / +0.694 (windows A/B), chain-whitened sd/lag-1 0.997/+0.04 and 0.997/+0.01 (the same residuals under the diagonal whitener read lag-1 +0.797/+0.688, matching φ̂ to two decimals), and the period lands at 40.3712/40.3696, so the D31 relocation (40.444/40.430) does not recur. The per-epoch jitters collapse from 1.11–3.61 to 1.55–1.93, their medians (1.66/1.40) reproducing the D30 residual sds (1.674/1.403): D31's jitters were fitting correlated structure as per-exposure scale, and modeled as correlation the excess is roughly uniform across epochs, which is a pipeline property. The independent windows move 3.0× closer in period (ΔP 0.00475 → 0.00159 d; D31 had widened it to 0.0145 d), the first noise model that makes the windows agree more rather than less. K_Be moves toward the literature (1.93→2.45, 2.95→3.76, against 3.90±0.27; window B is now within 1σ). The literature period offset (~0.044 d) survives its third noise model, measured to be neither continuum, nor scale, nor correlation; the survivors are the Gaussian LSF stand-in, disc variability, and the published CCF blending itself. At φ = 0.7–0.8 a diagonal model overcounts low-frequency information by (1+φ)/(1−φ) ≈ 5.5–9×, which is the mechanism behind D30's ±0.0005 d formal errors coexisting with 0.005 d window disagreements | benchmarks.md "D34"; `tests/test_ar1.py`; `scripts/hr6819_ar1_run.py`; math.md §1.4a |
| D35 | Band assembly extended to the AR(1) chain (pulls the lever D34 recorded; prerequisite for the wider window) | D34's condition, that the tridiagonal sandwich be built only if AR(1) earns permanence, was met by its science verdict, and the wider window could not be paid for on the probe path (gradient peak 23.7 GiB at current window scale on a 32 GB machine). The only stage of the D28 assembly that assumed diagonal noise was the innermost sandwich `H = R^T W' R`; the chain adds one symmetric cross-row term per link, `−c·√(w_n w_p)·r_n r_p·(R_n^T R_p + R_p^T R_n)`, and re-weights the diagonal by the chain diagonal `1 + Σa` (math.md §4.5a). This is carried by a second set of static pair tables (`operators.rebin_link_pair_tables`) built at `build_problem` time over the union of links realized in any epoch; masks differ per epoch, so a per-epoch gap test (`ar_gap[e, link_row] == link_gap`) selects each epoch's own links against the shared tables, and restricting the tables to realized links is also what keeps them inside the `ar_step` width bound. Upper-band storage gets both transposes from the two orderings of each entry pair, doubled where they coincide on the diagonal. `H` widens by the group's static `ar_step`, the same quantity `ar_bandwidth_extra` already reserved, and everything downstream is untouched (LSF convolutions, T-sandwich, band accumulation, D29 chunking, custom-VJP solve). `marginal_loglikelihood` auto-selects `"band"` unconditionally; probing stays as the reference implementation and `validate` oracle. Exactness: the D34 dense-LAPACK gold test runs the band path at rtol 1e-10 unchanged; band = probe at rtol 1e-12 with ∂/∂φ to 1e-9; the `epoch_chunk` batching invariant is pinned, because the AR weight tuple (diagonal, link, gap) pads and slices together and a batched run dropping link terms would fail silently. Measured at HR-window scale (51 ep, 9.8k model px, 367k native px, CPU): eval 5.10 → 0.71 s (7.2×), value+grad 58.83 → 3.07 s (19.2×), gradient peak working set 23.7 → 1.85 GiB (12.8×), results identical to all printed digits. The mechanism is the D28 one: the band path's custom-VJP solve against plain reverse mode through 2p+1 = 347 probe applications. The window A refit is the real-data check: 8,369 → 868 s end-to-end (9.6×), same optimum, log-likelihood 1,691,463.5 to the printed digit, P within 0.00002 d (0.02 formal σ), φ̂ +0.801, α range and residual diagnostics identical | benchmarks.md "D35"; `tests/test_ar1.py`; `scripts/d35_ar1_band_bench.py`; math.md §4.5a |
| D36 | The wide window: one joint HR 6819 fit, 4120–4600 Å with the Hγ core masked (the last lever D30/D33 recorded; spends the D35 speedup) | Joins windows A and B (22,169 model px, ~765k good native px, 2.26× window A, including the never-fitted 25 Å strip 4355–4380) with Hγ's core (4325–4355) masked by `preprocess.mask_ranges`: ivar = 0 keeps the sampling regular, the AR(1) chain restarts across the hole (masked gap beyond `ar1_max_gap`), the bandwidth is untouched, and the broad absorption wings (static stellar features) stay in. D34 noise model and procedure exactly; 200 L-BFGS steps, 2,447 s at 12.2 s/step on the D35 band assembly, linear in pixels from the window A refit, where the probe path would have cost ~7 h against a ~50 GB gradient. Results: P 40.36750, K 63.396/3.482, e 0.0261, φ̂ +0.737, α 1.46–1.86 (median 1.61), chain sd/lag-1 0.997/+0.019, diagonal lag-1 +0.731 ≈ φ̂ (self-consistency, a third time). Readings: the noise model closes at 2.3× the data, with φ̂ and α̂ between the single-window values (a pipeline property varying mildly with wavelength); K_Be firms toward the literature at 3.48 against 3.90 ± 0.27 (1.5σ), where window A alone gave 2.42, so the §5.1 data-starved direction responded to more lines; e sits at 0.5σ; the period lands below both single-window optima (40.3675 against 40.3711/40.3696), since a joint fit is not an average of window MAPs and the multimodality lesson applies, moving toward the literature but still 0.041 d away. The literature period offset survives its fourth configuration: two windows, three noise models and one wide joint fit give P ∈ [40.3675, 40.3712] (internally consistent to 0.004 d) against a published 40.3261 ± 0.0013, with K_pre-sd 63.2–63.5, consistently 3.7–4% above the CCF value in the deblending direction. Survivors: the Gaussian LSF stand-in (the next lever; D8's tabulated-LSF v2 seam, a banded-operator swap), disc variability, and the published CCF blending itself | benchmarks.md "D36"; `scripts/hr6819_wide_run.py` |
| D37 | The tabulated-LSF seam opened (D8's v2; the top surviving D36 suspect) | The kernel slot becomes a profile bank: one row is the stationary v1 operator (a bit-identical code path), `grid.n` rows give a wavelength-dependent LSF, with per-anchor kernels linearly interpolated in log-λ through static tables (`operators.lsf_anchor_tables`) and applied by the row-varying convolution `operators.convolve_varying` (`K[m,c] = P[m, m−c+r]`; exact adjoint; arbitrary asymmetric banks are accepted, and the θ-parameterization is per-anchor Gaussian widths, `lsf_sigma` with one entry per anchor and build widths as per-entry radius bounds). The band assembly keeps its structure: scalar sandwich taps become row-shifted profile columns, and since only left applications broadcast on a row-major band image, the second application runs against the band-transpose of the first, `G = Kᵀ(KᵀH)ᵀ` by G's symmetry (math.md §4.5); same width, same flops. Band = probe = dense at rtol 1e-12/1e-10 under diagonal and AR(1) noise; per-anchor gradients band = probe at 1e-9; random asymmetric banks pin any hidden kernel flip; the simulator injects through the same recipe. The closed loop measured the identifiability (math.md §1.3): a stationary kernel change commutes with the shifts and is absorbed by the free spectra, so the marginal does not prefer an injected width ramp over a flat width (−3 nats). Fitted widths are therefore diagnostics rather than measurements; the identified content is the anchor-to-anchor variation and the orbit's response, and the joint fit leaves the orbit unbiased while recovering the ramp's direction. On HR 6819 (13 anchors × 40 Å on the D36 configuration): +90.5 nats and the orbit does not move. P 40.36769 (+0.0002 d, 0.5% of the offset), K₁ 63.395, e 0.0262, φ̂ +0.737, α̂ and both residual moments unchanged to the third digit; σ̂(λ) reads 2.15 at the blue edge then 3.1–3.4, close to the 3.5 bound, which is the closed-loop absorption behaviour on real data; K_Be is unconverged at 200 steps (oscillating 1.30–4.16, a band spanning D36's 3.48, because the 13 near-flat width directions slow the flattest axis; 28.3 s/step, 2.3× D36). This repeats D33's pattern for the LSF. The literature period offset survives its fifth configuration; LSF width variation is exonerated, and the surviving LSF suspect narrows to profile asymmetry (first-order centroid channel ∝ λc′(λ)v(t)/c; the operator is ready, and only a θ-parameterization, for example per-anchor Gauss–Hermite h₃, would be new), alongside disc variability and the published CCF blending | benchmarks.md "D37"; `tests/test_lsf_varying.py`; `scripts/hr6819_lsf_run.py`; math.md §1.3, §4.5, §7.5 |
| D38 | The asymmetry lever: per-anchor Gauss–Hermite h₃ (closes D37's surviving LSF suspect) | The θ-parameterization for the operator D37 left ready: `operators.gauss_hermite_kernel_traced` (van der Marel & Franx 1993 truncated at H₃, unit sum, h₃ = 0 bit-identical to the Gaussian), threaded through `gaussian_lsf_profiles`/`build_problem`/`with_lsf` for anchored instruments only, since a stationary asymmetry is absorbed by the free spectra and un-anchored instruments therefore get no slot, with site `lsf_h3` (per-anchor, concatenated across anchored instruments, clipped and guarded at \|h₃\| ≤ 0.2, and admissible without `lsf_sigma`). Exactness: band = probe = dense with h₃ anchors under diagonal and AR(1) noise; ∂/∂h₃ band = probe at 1e-9. The closed loop measured the identifiability, which is sharper than the width case: an injected ∓0.12 h₃ ramp came back flat (fitted \|ĥ₃\| ≤ 0.03) with the orbit recovered to 1%, because a free spectrum represents any static centroid-warp field c(λ) ≈ √3 h₃ σ outright. The data-identified remainder is the epoch-coupled warp-gradient sampling Δc ≈ c′λ(v − v_bary)/c ≈ 30 m/s here (math.md §1.3 records the estimate, and why the instrument-frame per-epoch kernel realization is bounded out rather than built: the channel it adds is two orders below the offset's ~4 km/s accumulated signature). On HR 6819 (13 h₃ anchors joint with the D37 widths, 300 steps, 36.0 s/step): +26.6 nats and the orbit does not move. P 40.36719 (−0.0005 d from D37, inside the trajectory wobble), K₁ 63.391, e 0.0261, φ̂ +0.737, both residual moments unchanged; ĥ₃ is ≈ 0 in the interior with 0.04–0.05 at three anchors (centroid shifts ≤ 0.31 km/s, diagnostics); K_Be is again unsettled on its flat axis (2.73 at \|grad\| 44). The offset survives its sixth configuration and the LSF is exonerated in full: every instrumental channel this model can express, namely continuum (D33), noise scale (D31), pixel correlation (D34), window (D36), LSF width (D37) and LSF asymmetry (D38), is measured not to move the period (P ∈ [40.3672, 40.3712] against a published 40.3261 ± 0.0013). The surviving suspects are no longer instrumental: the Be disc's variability and the published CCF blending. The instrumental-systematics campaign on this dataset is complete | benchmarks.md "D38"; `tests/test_lsf_h3.py`; `scripts/hr6819_h3_run.py`; math.md §1.3, §7.5 |
| D39 | The usability layer: results, plotting, packaged example data (the Tier-1 roadmap block) | Six defaults recorded so that they are not relitigated. (1) Serialization is `.npz` with a JSON header, not netCDF or HDF5. All four result types are flat arrays plus a few scalars, so `load_fit` reconstructs by calling the constructor with keywords: no pickling, and no pytree aux-data serialization to keep in step with the classes. netCDF would pull in xarray and h5netcdf and FITS would pull in astropy; NumPy is the only hard dependency that can write a container. A `__format_version__` in the header makes a future reader refuse rather than misread. (2) `MarginalResult` saves `d_hat` and `spectra_std` by default, not the precision: the blocks are GB-scale at the design target (D29), and a default that dropped the uncertainty band would drop the main product; `precision=True` round-trips exactly. Consequently `K2ScanResult.model` is now optional, since a scan read back from disk has no live `MarginalOrbitModel`. (3) matplotlib and arviz join astropy as optional extras, lazily imported through the package `__getattr__` so that `albireo.plot_spectra` stays discoverable and raises an actionable message rather than an ImportError from inside a figure; a fit on a headless node should not need a plotting stack. CI installs with no extras into a second environment and asserts the guard, because an untested guard is only an intention. (4) The packaged example is eccentric (e = 0.15) rather than circular. The (√e cos ω, √e sin ω) parameterization is singular at exactly e = 0: ω is undefined, e·cos ω behaves like \|x\|, and the gradient is NaN at the origin, whereupon numpyro reports "Cannot find valid initial parameters", which does not point at the cause. A circular example would put both the truth and the obvious starting guess on that point, so the first thing a new user runs would fail. This is documented in the quickstart; a guard in `run_map` is a recorded lever, not built. (5) The example ships inside the wheel (124 kB, seeded, regenerated by `scripts/build_example_datasets.py`) and the cache/download path is stdlib `urllib` and `hashlib` rather than `astropy.utils.data`, because `load_example` is the first call a new user makes and requiring an extra for it defeats the purpose. (6) A bare `pytest` still runs the acceptance gates; `--no-slow` deselects them (14 min → 6 min) and the routine CI job passes it while the on-demand `Full` workflow runs everything with coverage. The gates are what the closed-loop tolerances rest on, so a default that stopped checking them would be the wrong default. Two pre-existing defects surfaced, both invisible because nothing had been pushed and CI had therefore never run: `pytest` (the console script) could not collect three modules that import shared helpers as `tests.test_likelihood`, and `ruff format --check` did not pass on a clean tree | benchmarks.md is unchanged (no numerics moved); `tests/test_results.py`, `tests/test_plotting.py`, `tests/test_examples.py`, `tests/test_metadata.py`; roadmap.md; releasing.md |
| D40 | The nebular component, and per-pixel prior strengths (Tier-2 roadmap item 1) | The component: `build_problem(nebular=True)` appends a column that is static in the barycentric frame, the mirror of the telluric law, so it is the topocentric-frame data that carry the per-epoch `-v_bary` term, with a free per-epoch amplitude in place of a light fraction. That last point follows from the physics: nebular flux is added on top of the total stellar continuum and takes no light from the stars, so its amplitude does not belong on the simplex, and its night-to-night variation (seeing, slit losses, sky subtraction) is one scale on one fixed shape. Structurally it is rank-one time variation, the same form that Tier 3's variable-disc component generalizes, which is why that generalization stays inside the linear-Gaussian family. It is swapped in by `forward.with_nebular_amplitudes`, θ site `log_nebular_amp`. Component order is now stellar, telluric, nebular, and the two θ-path swaps that used to rebuild the telluric column now carry the trailing columns through unchanged, which is shorter and correct for any number of non-stellar components, since their velocity laws depend only on the frame and `v_bary`. Two degeneracies are closed by convention rather than by data. Scale: only `a_j·d_neb` is observable, so the prior would otherwise be left breaking a nearly flat unbounded direction. The site is centered, `a_j = exp(u_j − mean u)`, pinning the geometric mean to 1; the prior on it is then a prior on epoch-to-epoch variation, and the level lives in the spectrum. Velocity: the nebular shift is epoch-constant (barycentric) or differs only by `v_bary` (topocentric), and a constant shift of a free spectrum is a reparameterization, so `nebular_v_kms` is not identified, exactly as γ is not (D14). It survives only because it decides where the component's lines land on the model grid, which is what the window profile below has to agree with. Both are stated at the call sites. The prior grew a per-pixel axis because confinement requires it: `SmoothnessPrior` takes optional static `tau_profile`/`eta_profile` multipliers, with the inferred scalars kept separate so ML-II is untouched (a profile says where, the scalar says how much). The pentadiagonal entries generalize to `d0 = t_a + 4t_{a-1} + t_{a-2} + e_a`, `d1 = −2(t_a + t_{a-1})`, `d2 = t_a` with curvature rows weighted by their center pixel: same bandwidth, same O(P) determinant recursion, same band assembly, and bit-identical to v1 at a uniform profile. Because the split is structure against strength, an inferred `(tau, eta)` keeps the construction-time profiles: `MarginalOrbitModel` merges the sampled scalars into the fixed prior's profiles rather than replacing the whole object, which is the difference between a windowed component and one that is un-confined the moment ML-II is switched on. `priors.window_profile` plus `nebular_windows` and `NEBULAR_LINES` build the confinement: `eta × 1e6` outside the line windows pins the component to the continuum there (prior sd 1e-3 of the in-window value) while leaving the precision better conditioned. It is soft rather than hard, because a hard zero needs different linear algebra and removes the model's ability to disagree. Measured. Closed loop: an SB2 (K = 58/41 km/s, ℓ = 0.7/0.3, 12 epochs) whose Hβ absorption carries a static nebular line of peak 0.45 varying ±30% per epoch. Disentangled without a nebular component, the mean flux in the line core rises by +0.154 against a true depth of −0.506 (the recovered core bottoms out at −0.375, 26% shallower) and the Hβ equivalent width comes out 11.5% low (1.690 against 1.911 Å). With the component: +0.0015, −0.508, and 0.14%, marginal likelihood ahead by 8.1×10⁴ nats, with recovered amplitudes correlating with the injected ones. The equivalent width is the consequential number, because equivalent width is what reaches the atmosphere code: this is a systematic error in log g that nothing in the current literature propagates. The orbit is affected more strongly still: the same data fitted jointly from a cold start gives K₂ 16.77 against 41.0 (−59%), P +0.171 d, and a circular orbit reported at e = 0.95, the solver's clip, without the component, against K₂ −0.29%, ΔP −0.00014 d and e 0.0022 with it; the blind fit is also still at \|grad\| 2.9e4 where the modelled one has settled at 8.3, and takes 3.6× the wall time to fail. A static line is a component with K = 0, so a model with nowhere to put it uses whichever star can be made to move least; only K₁ survives, pinned by 70% of the light. Nebular contamination therefore propagates into the dynamical answer, that is into masses, and not only into the atmospheric one. Per-epoch amplitudes recover at corr 0.99930, 0.0066 rms in log against an injected 0.78–1.50× spread, and ML-II independently keeps the nebular component less smooth than the stellar ones. The window profile is measured in that same fit: free across the whole grid the component takes K₂ to +2.6% and the potential 250 nats worse, because the freedom it was given was spent absorbing stellar signal where a nebula has no lines, which is the original failure one level up. The forward model reproduces the simulator's injection to 1e-12 in both frames, and band = probe with the nebular column and a window profile present | benchmarks.md "D40"; math.md §1.3, §2, §5.4; `tests/test_nebular.py`; `examples/04_nebular.py`; roadmap.md Tier 2 item 1 |
| D41 | Calibrated faint-companion detection: a vectorized scan, K₁ marginalized, and an empirical false-alarm rate (Tier-2 roadmap item 2) | Vectorization comes first, because the other two are unaffordable without it. `MarginalOrbitModel.log_likelihood_sweep` runs a grid of trial θ values as a single batched `lax.map`, one compiled graph with no per-point device synchronization, using the same two-regime batch policy `_epoch_chunk_default` uses for memory. Measured 2.0–2.8× across problems from 201 to 2,652 model pixels, near-flat in size, so the gain comes from the batching rather than from removing the dispatch, and agreeing with the loop to 1e-12 relative or better. What it buys: a 7×20 (K₁, K₂) grid costs 0.88 s and a 450-scan calibration (9,450 marginal solves) costs 53 s. K₁ is a nuisance parameter, and conditioning on it is not free. `k2_scan(k1_sigma=)` integrates it out over a Gauss–Hermite rule on N(μ₁, σ₁²), applied to the companion and to the no-companion model so that D remains a ratio of two marginal likelihoods rather than a comparison of differently conditioned ones; `k1_sigma=None` is the one-node rule and reproduces the previous behaviour exactly. The measured failure mode is worse than the literature's description of it. The papers report that a small error in the assumed primary semi-amplitude puts spurious features in the recovered secondary. The closed loop shows that D rises while that happens: unremoved primary signal is coherent across epochs, the companion's free spectrum is the only thing that can absorb it, and it does. K₁ 5% high took the companion's recovered line pattern from 0.96 to 0.72 correlation with truth while D went 40,609 → 66,837, and K₁ 10% high gave 0.49 and D = 135,410, so the artifact reads as a stronger detection. Marginalizing restored 0.955 and 0.931 with D back at ~41,000, and recovered the correct K₂ peak where the fixed 5%-high scan had it one grid step low. Peak spectra stay conditional on the best node (a profile): there is no closed form for the K₁-marginalized spectrum, and averaging the nodes' spectra would blur the lines instead of widening their error bars. The calibration is a parametric bootstrap through the observed data's own operators. `forward.with_data` swaps the data term z = y − r(R1), the only place the fluxes enter, and `simulate.resimulate` redraws it from the problem's own forward model, so epochs, v_bary, chip gaps, cosmics, native wavelength solutions, response and per-pixel weights are the real ones and each trial costs one forward apply instead of a fresh `build_problem`. `calibrate.detection_limit` scans N companion-free draws for the null distribution of the maximum over the grid, which is the statistic a search reports, rather than D at one trial K₂, and a ladder of injected ℓ₂ for completeness. Two properties are enforced rather than assumed. The threshold is defined through the FAP estimator (1 + #{null ≥ D})/(N+1) rather than as a sample quantile: `np.quantile` interpolates between order statistics and can leave 8% of the null above a nominal 5% threshold, which a test catches, and which is anti-conservative in the direction that matters for a detection claim. And no FAP below 1/(N+1) is ever reported; when the requested rate is finer than the trials resolve, the rule degrades to "must exceed every null trial" rather than inventing precision. A ladder whose faintest rung is already complete sets `limit_is_bracketed=False` and `summary()` says *at most*, because reporting the first rung would look identical to a measured crossing. What this does not do is check the model. The null trials are drawn at the same K₁, orbit and light fractions the scan assumes, so the threshold is self-consistent with those assumptions and blind to their being wrong, which is why the K₁ marginalization exists beside it. The limit is also conditional on the assumed companion template (the observable is ℓ₂·d₂, so a featureless companion is invisible at any light fraction); the default is the primary's own recovered spectrum, and the assumption is required to be quoted with the number. Measured on a 717-pixel, 14-epoch, SNR-200 SB1: null peaks are strictly negative (the Occam term, so "D > 0" would have been a conservative test here and might not be on another dataset), and the search is sensitive below 0.5% of the light at 95% confidence | benchmarks.md "D41"; math.md §6.1, §6.2; `tests/test_calibrate.py`; `examples/05_detection_limit.py`; roadmap.md Tier 2 item 2 |
| D42 | The free per-epoch radial-velocity table (Tier-2 roadmap item 3) | A ``velocity`` theta site of shape (n_stellar, n_epochs) that replaces the Keplerian rather than supplementing it (mixing the two raises), turning the joint fit into the diagnostic mode the design has listed since M3 and never exposed. `forward.with_shifts` was split out of `with_velocities` as the pixel-space core, and `LogGrid.pixels_to_velocity` added as the exact inverse of `velocity_to_pixels`. The identifiability is the substance of the decision, and it is not what it appears to be. A free table has one arbitrary zero point per component, not one in total: with no orbit tying the components together, each component's free spectrum absorbs a constant added to that component's shifts. This is therefore gamma (D14) once per star. Measured before building anything on it: a whole-pixel common shift of one component moves the log-likelihood by 4e-9 in relative terms (boundary effects only), but a 0.1-pixel one costs 7.3 nats, because the model shifts by linear interpolation and a fractional shift blurs as well as moves. Left alone, a table's absolute level would therefore be pinned by interpolation error: a number that looks like a systemic velocity, moves when the grid is resampled, and means nothing. albireo removes it. The centering is done in pixel space, which matters. With the relativistic mapping xi = artanh(v/c), chosen in M1 because shifts then compose and invert exactly, a constant pixel offset is relativistic velocity addition rather than ordinary addition. Centering the pixel shifts makes the likelihood exactly invariant (measured: dlogL = 0.000e+00 at 5, 50 and 200 km/s on either component); centering the velocities instead leaves a residual four to six orders of magnitude larger, and the test pins the difference rather than asserting the choice. `inference.relative_velocities` maps the centered shifts back to km/s through the exact inverse, so the reported table is each component's velocity relative to its own mean epoch, with the variation, and therefore each semi-amplitude and the Wilson slope, which is a slope and so immune to both zero points, untouched. The uncertainty needs the same projection, and this is the trap worth recording. Reading the Laplace covariance's diagonal returns the prior and nothing else: with Normal(0, 120) over 10 epochs every raw marginal sigma came out at 37.95 = 120/sqrt(10), identical to four digits across both components and all epochs, against a per-epoch error of 0.059 km/s, a factor of 640, and insensitive to the data, so it would look equally convincing on a useless dataset. `relative_velocity_errors` projects each component's mean out of the covariance; the projected block then has exactly n_stellar zero eigenvalues, which confirms the identifiability claim numerically rather than by argument. Posterior samples need none of this, since the numpyro model records `velocity_rel` as a deterministic, and that route is what the docs steer to. Measured: 10 epochs, SNR 200, 400 model pixels at dv = 6 km/s, K = (30, 55), ell = (0.6, 0.4). Warm-started from a Keplerian 30% wrong in both semi-amplitudes, the fit lands at per-epoch RV rms of 0.098 / 0.066 km/s, 1/60th of a model pixel, recovers the Wilson slope to 0.4% (-1.8255 against -1.8333 = -K2/K1), and threads the generating Keplerian at max residual 0.164 km/s (2.8 sigma) where a period wrong by 0.5% gives 2.98 km/s (50 sigma). Starts at the truth, at 10% error, at 30% error and at truth-plus-15-km/s-noise all reach the same optimum to four decimals. The failure mode is left visible rather than papered over: from a cold start with every epoch at one velocity the components are indistinguishable and the fit lands at rms 12.9 / 28.7 km/s with a slope of the wrong sign, but at a potential 122,000 nats worse, so it cannot be mistaken for a fit. The mode is documented as needing a warm start, which is also how it is meant to be used: fit a Keplerian, then free the velocities and ask whether the Keplerian still threads them (`inference.keplerian_residuals`, which centers both tables the same way and differences them in pixel space so the two arbitrary zero points cancel exactly) | benchmarks.md "D42"; math.md §7.6; `tests/test_velocity_table.py`; `examples/09_rv_table.py`; roadmap.md Tier 2 item 3 |
| D43 | Air vs vacuum wavelengths become a declared, validated property | `EpochData.medium` ('air' / 'vacuum' / None) plus `grids.air_to_vacuum` / `vacuum_to_air`, and a `Dataset` guard that refuses a mixture instead of picking one. Pulled forward out of roadmap item 7 because item 4's archive loader forced it: ESO Phase 3 declares the scale per file in `TUCD1` ('em.wl;obs.atmos' = air, bare 'em.wl' = vacuum), and the six instruments the loader targets are split: FEROS, HARPS, UVES and GIRAFFE deliver air, ESPRESSO and XQ-100 vacuum. A loader reading both without a field to record which would have been silently wrong. The size of the error is the argument. In Angstrom the offset grows with wavelength (0.87 A at 3000, 2.74 A at 10000), but as a velocity it is nearly constant at 83 km/s: 87.4 at 3000 A, 82.8 at Halpha, 82.2 at 10000 A. That is the same order as the semi-amplitudes albireo exists to measure, it is systematic rather than random, and it does not average out over epochs. None means undeclared, and is not a synonym for air. Every epoch built before this field existed means None, so it stays legal and the default is backward compatible, but a Dataset mixing None with 'air' raises just as loudly as one mixing 'air' with 'vacuum', because 'unknown' is not a value that can be checked against anything. The alternative, defaulting to 'air' because most optical spectrographs deliver it, was rejected: a wrong default that raises nothing is the failure mode the field exists to remove, and it would have made every pre-existing epoch assert something nobody had checked. The conversion is the IAU-adopted Edlen (1966) / Birch & Downs (1994) refractivity evaluated at the vacuum wavenumber, which is the convention Morton (2000) tabulates and therefore what published air line lists agree with. Inverting it is not closed-form, so `air_to_vacuum` iterates a fixed point twice; the refractivity moves by ~1e-8 over the 0.03% the wavelength does, so one pass is already good to ~1e-11 A and the round trip closes to float64 (measured: max error exactly 0.0 over 3000-10000 A). Tested against an independent plain-NumPy transcription of the published coefficients rather than against remembered line pairs, which come from different sources with different conventions and are not self-consistent to the milli-Angstrom; a first draft of the test failed at Halpha for that reason (the code was right, the remembered 6562.819 was not, the value is 6562.80) | `tests/test_wavelength_medium.py`; roadmap.md Tier 2 items 4 and 7 |
| D44 | An ESO Science Archive client in the package (Tier-2 roadmap item 4, part 1) | `albireo.archive`: `spectra_query` builds the ADQL, `query` runs it, `download` fetches, and `scripts/download_hr6819.py` collapses from 180 lines to a thin wrapper, which shows that the generalization covers the case it came from. Stdlib only (urllib, json, concurrent.futures): finding data should cost no dependency, and astropy is needed only to open a file. Three archive behaviours drove the design, each measured against the live service rather than assumed. (1) Cone-search, never name-search. `target_name` is PI free text and is not resolver-normalized (HR 6819 is filed as `HR-6819`), so a loader must resolve to coordinates elsewhere and search a circle. For 1-D spectra `s_region` is a bare `POSITION J2000 ra dec`, a point rather than a footprint, so the `CONTAINS(POINT(...), s_region)=1` form in ESO's own documented examples matches nothing. A network-marked test asserts both halves against the real archive (INTERSECTS returns the 51 spectra, CONTAINS returns zero) and is written to fail loudly if ESO ever changes it. (2) Truncation is silent. The sync endpoint caps output at 20,000 rows and its JSON and CSV serializations carry no overflow marker; only VOTable does. A capped result is indistinguishable from a complete one, so `query` compares the row count against `maxrec` and raises rather than handing back a short epoch list, which would give a confidently wrong orbit rather than an obvious failure. (3) An archive row is not necessarily one epoch. GIRAFFE writes one file per science fibre, up to 130 per raw frame, and multi-epoch stacks with `M_EPOCH=True` span weeks; the client returns the metadata to filter on and declines to guess. Four defects in the original script were found by reading it and are fixed here: `access_estsize` is nullable and was dereferenced outside the per-file try, so one null row killed a whole run; `Content-Length` was never checked, so a transfer cut cleanly in the middle landed on disk looking complete; `OSError` was in the transient-retry tuple, so a full disk was retried five times and then reported as network trouble; and the manifest was written before the downloads, so it could list files that subsequently failed, and is now written afterwards with each product's outcome. The retry policy (5 attempts, 2-4-8-16 s backoff, fast-fail on 4xx except 429) and the `.part` plus atomic-rename commit are kept as they were, both being correct. Downloads default to 4 concurrent: ESO publishes no rate limit, so that is a conservative choice rather than a tuned one, and the docstring says so | `tests/test_archive.py` (18 offline + 2 network); `docs/api/archive.md`; roadmap.md Tier 2 item 4 |
| D45 | Read the archive's own format, and resolve a survey target by name (Tier-2 roadmap item 4, part 2) | Two halves of one goal: turn a survey identifier into fitted spectra without a hand-written loader. The reader now dispatches on IVOA utypes (`TUTYPn`) rather than on column names. Thirteen real Phase 3 spectra across seven instruments were dumped column by column, and no two collections agree on anything else: flux is `FLUX` on HARPS, `FLUX_REDUCED` on GIRAFFE and both on X-shooter; the extension is `SPECTRUM` except for Gaia-ESO's `phase3spectrum`; units are `angstrom`, `Angstrom` or `nm`. Dispatching on UCDs instead is unsafe, and that is the finding worth keeping: UVES labels its sky-background column `phot.flux.density;em.wl;stat.uncalib`, byte-identical to the UCD on the HARPS flux column, so a UCD-keyed reader hands the solver the sky. Only the utype role separates `BackgroundModel.Value` from `FluxAxis.Value`. Utypes are matched by the suffix after `Data.` because the namespace is `spec:`, `Spectrum.` or `eso:` depending on SDP version, and because ESO misspells `Accuracy` as `Accurancy` in ESPRESSO and GIRAFFE but not X-shooter; names survive as a last-resort fallback. The reframing that mattered: all 13 files already read. What was wrong was the metadata and the weights, so the work was ranked by what a user could observe being wrong. `medium` was the worst: D43 added the field, `to_epoch` never passed it and `preprocess._replace` dropped it on every trim, so the 83 km/s guard could not fire; it is now plumbed through and `read_dataset` refuses a mixture. Quality columns were ignored (97% of X-shooter's flagged pixels entered the fit at above-median weight); a zero uncertainty was read as infinite precision rather than as "nothing here"; an all-NaN `ERR` (FEROS, HARPS) was swallowed without a warning instead of announcing that the weights are now albireo's assumption; `v_bary` fell back to a fabricated 0.0 without a word; `ESO TEL TARG ALPHA` is packed sexagesimal hours and was read as degrees, wrapping to a plausible wrong position; `MJD-OBS + EXPTIME/2` is not the mid-point of a 30-day 9-exposure coadd, so `TELAPSE` now vetoes that fallback rather than guessing; and a one-character `R` keyword could pass for a resolving power. Bad pixels are also kept out of the continuum fit, since a flagged cosmic lifts the upper envelope into every line depth. BLOeM resolves by name (`resolve_bloem`, `bloem_catalogue`, `bloem_spectra`) because the archive does not know the survey's names: spectra sit under `obs_collection='GIRAFFE'` with `target_name` set to the Gaia DR3 source id, and the cross-match is in VizieR, which speaks the same TAP dialect, so the join costs no dependency. Ids are kept as strings: 809 of the 929 fail a float64 round trip. An unplanned finding was a second programme. 23,651 spectra match `112.25R7%`, but the same 929 stars return 25,478 rows unfiltered; the extra 1,827 are `115.28A9`, a follow-up at *R* = 17000 and 23000 in two other windows against the survey's LR02 (3960-4571 A, R = 6300). Pooling them would hand the model three disjoint windows under one LSF, so the default is the survey programme and the alternative is a documented parameter. Not done, and recorded: heliocentric stays aliased to barycentric (6 m/s on BLOeM against a 48 km/s resolution element, and a third frame would reach the physics core for nothing), with the raw `SPECSYS` recorded instead. An adversarial review of the change caught five defects it had introduced, and the pattern in them is the reason for this row: every one was a guess presented as a reading. (1) Honouring quality flags assumed the standard's "zero is good", but UVES_SQUAD's `STATUS` runs {-5, 1} and never takes the value 0, so all 467 of that collection's products read as 100% bad and raised; a flag column in which zero never appears is now dropped with a warning, and `MASK`/`FLAG` are not read at all because those names carry no agreed polarity (albireo's own `EpochData.mask` is True = good, the opposite). (2) Ranking flux candidates by namespace encoded "eso: is the raw one", which XShootU inverts, with science flux in `eso:` and a derived telluric-corrected column in `spec:`, so the key is deleted and the two UCD keys decide every real case. (3) The new all-bad guard was evaluated on the padded continuum window, so a dead region beside a live pad produced a zero-weight epoch that the solver accepts without complaint. (4) `SpectralAxis` names frequency and energy axes too, and one would have been read as Angstrom. (5) The fix letting `bjd=` stand in for a missing time only worked if `v_bary=` came with it, so the error advising `bjd=` was still unreachable. Each is now a test. The review also corrected a claim in the codebase itself: the UVES sky-background column is excluded by its `BackgroundModel.Value` utype, but FORS2's sky is typed `eso:Data.FluxAxis.Value` and is excluded only by `meta.main` on the real flux, so the role filter is not the whole guard the comment claimed | `tests/test_sdp_dispatch.py` (25 offline + 4 network); `docs/api/io.md`; `docs/api/archive.md`; `examples/06_bloem.py`; roadmap.md Tier 2 item 4 |
| D46 | The `Disentangler` façade: a declaration compiles to the expert path (Tier-2 roadmap item 5) | `albireo.facade`, marked experimental, adds 14 names: `Disentangler`, the components `Star`/`Telluric`/`Nebular`, the declarations `Orbit`/`LSF`/`Smoothness`, the specs `Fixed`/`Known`/`Between`/`Scanned`/`Sampled`, and the results `Fit`/`Posterior`. The framing that made it tractable is that it is a compiler rather than a shortcut: it takes a declaration of the system and emits the low-level path, which `explain()` prints and `expert()` hands back as the exact `(model, priors, init)` triple, so support for both paths in parallel is structural rather than a promise. Four quantities are derived, each one something the low-level path leaves to the user. The velocity budget, from the `k` priors' own support: it must bound what the prior allows rather than what the answer turns out to be, and too small a budget stalls the sampler against an invisible guard. The grid margin, being the budget plus the LSF kernel radius, short of which the shifted model runs off the grid and loses flux without warning. The conjunction phase, from a 41-point scan before anything is optimized: 1.15e5 nats separate the best and worst phase on the packaged example, and L-BFGS in the wrong trough converges confidently to the wrong answer. And the smoothness hyperparameters by ML-II, reported per component with a drift flag on any that did not move from its start, the flag indicating that the hyperprior rather than the data set that component's smoothness. A fifth is structural: a spec carries its prior and its starting value, so `priors` and `init` cannot diverge, where in the low-level path they are two dicts written twice with an assertion between them. Five refusals, each at a place where a default would be a scientific claim. Light fractions are required per star and must sum to 1 (only `l_i * d_i` is observable, so the value is an assumption the data cannot contradict, and it is repeated in an `Assumed, not measured` block on every summary). There is no period search, only a phase scan at one period. An undeclared air/vacuum scale is refused when a `Nebular` or `Telluric` component makes it an 83 km/s question, and not refused for a plain single-instrument SB2, where a refusal that must be worked around would only teach users to work around refusals. A budget override smaller than the derived value raises, naming the term that overflowed. And the `e = 0` singularity is designed around rather than warned about: a free eccentricity never starts at the origin, and `ecc=Fixed(0.0)` is exact because those sites are not sampled at all, which converts numpyro's uninformative "Cannot find valid initial parameters" into a supported mode. The names rejected from the `docs/design.md` §6 sketch carry as much of the decision as the accepted ones. `Keplerian(t_peri=, ecc=, omega=, k1=, k2=)`: every one of those argument names is wrong post-D20, where the sites are `t_conj`, `secosw`/`sesinw`, and a vector `k`. `ab.Normal`/`ab.Uniform` as first-class names: wrapping numpyro forks the `.to_event()` batch-shape contract the sites depend on, and the specs are (distribution, init) pairs, which is the thing users get wrong. `FixedLight`/`light_ratio=`: "ratio" is the wrong word for a per-epoch simplex that generalizes to N components and excludes both non-stellar columns, so `light=` sits on the `Star` whose depths it scales. `PerEpoch(eclipse model)`: naming a model that does not exist is what the roadmap warns against. `GaussianLSF`: not Gaussian-only since D38, not one number per instrument since D37, and it reads as a value when the build-time width is a hard upper bound on the kernel radii. `dis.replace(...)`: it reads as cheap, but the transitions it invites are the ones the code raises on, and the one legitimate transition is `Fit.free_velocities()`, which hangs off a fit and is therefore unconstructible without the warm start D42 measured as the difference between working and 122,000 nats worse. And `degeneracy_report`: it promises a computation and would deliver a list of conventions. Measured on the packaged example: 12 façade lines against 59 expert lines, MAP `K` = 41.978 / 62.978 against a truth of 42 / 63, `e` = 0.1512 against 0.15, z-RMS 0.997, and NUTS `K2` = 62.988 +/- 0.081 with zero divergences. The risk is stated rather than mitigated: `Star(light=0.62)` sits in the same constructor as `period=Known(40.335, 0.5)` and is formatted identically, but one is a choice and the other a measurement, and a constructor slot teaches that everything in it is the same kind of thing. The `Assumed, not measured` block is a partial answer, because prose in an output nobody must read does not travel with the number into a table. An adversarial review of the finished module confirmed 38 defects, and the split is the lesson: the vocabulary survived unchallenged and the wiring did not. Not one accepted name was disputed; what failed was every place a declaration had to be translated into the model's own conventions. Two produced wrong science without any visible failure, of exactly the class this package exists to prevent. (1) The smoothness rows were assembled in declaration order while the model orders rows stars-telluric-nebular, so `[Telluric(), Star(broad, tau0=1e8), Star(sharp, tau0=1e3)]` regularized the broad star with the telluric's tau, with no symptom, because the vectors are still the right length and the only downstream guard is a length check; the two orderings of the same physical declaration differ by 9,900 nats. Fixed by a single `ordered_components` property used at every order-sensitive site. (2) `ecc=Between(lo, hi)` was not the prior that was fitted: the two sites are bounded independently, so the box corner reaches `e = 2*hi` (a declared 0.08 returned a MAP of 0.146), and `lo` was validated and then never referenced (a declared 0.3 returned 0.151). The bound is now enforced through the model's own disk factor via `effective_ecc_max`, and a non-zero lower bound is refused because it is an annulus in that plane rather than a box. The rest share one shape: v1 accepted declarations it could not honour. Three are now refusals rather than approximations: hierarchical triples, Gauss-Hermite `h3` (reachable through `build_problem` but not through the model class the façade builds, so the field was silently discarded and is now absent), and that eccentricity lower bound. The others were fixed: the budget sized a `Sampled` k from its starting value; the grid took the median epoch sampling rather than the finest, so a mixed-instrument dataset followed the epoch-count majority; `expert()` handed out a `t_conj` prior anchored at JD 0 when real epochs sit near 2.46e6; `dataclasses.replace` inherited the derivation cache, so a narrowed declaration kept the old grid and model; `free_velocities` dropped the nebular amplitudes and raised on any fixed orbital site; `velocity_errors` could never succeed; and `sample()` on a free-velocity fit returned a Keplerian posterior fitted from a random start. The generalizable lesson: a façade's risk is not in choosing names but in the translation layer underneath them, and that layer is where nothing downstream can check the result, since every one of these defects passed every existing guard | `tests/test_facade.py` (42 offline + 5 slow); `docs/api/facade.md`; `docs/quickstart.md`; `examples/07_facade.py`; roadmap.md Tier 2 item 5 |
| D47 | `sensitivity_forecast`: what the next epochs buy, before they are taken (Tier-2 roadmap item 6) | `albireo.forecast`, three new names: `sensitivity_forecast`, `SensitivityForecast`, `plan_epochs` (plus `plotting.plot_forecast`). The feature rests on one structural fact: the posterior covariance of the spectra is `(Lambda_p + A^T W A)^-1`, and no flux appears in it. Fluxes reach the marginal likelihood only through `b = A^T W z` and `z^T W z`, which move the posterior mean and the evidence, never the covariance. So the module assembles the precision with `assembly.band_block_tridiagonal`, never forms `rhs`, and a regression test overwrites every flux with noise 100x the continuum and requires the forecast back bit-identical rather than merely close. `plan_epochs` builds epochs whose placeholder flux is exactly the continuum, so a planned dataset fed to a fit by mistake returns featureless spectra, which is visibly wrong rather than plausible. Three summaries, each exact and each quoted against the prior alone (the D42 lesson: a band that has relaxed onto the prior looks identical to one the data earned): the pointwise band by Takahashi; the worst-determined modes by subspace iteration on the banded factor; and `p_eff = tr[Sigma A^T W A]` from a single directional derivative of `log det` in the noise scale, since `with_jitter` is already that one-parameter family, so the exact trace costs one directional derivative instead of a stochastic estimator. (D49 turned that JVP into a `jax.grad` of the same scalar-to-scalar function, with a bit-identical returned `p_eff`; it was the only forward-mode site in the package, and the change was forced because the band assembly gained a `custom_vjp` and `custom_vjp` rejects `jax.jvp`. It costs 0.28 s → 0.53 s once per forecast, and makes every leapfrog gradient 1.8x faster; `test_p_eff_matches_dense_trace` pins the value against a dense trace oracle either way.) Ranking whole designs is `0.5 (logdet Lambda_t - logdet Lambda_p)`, the expected prior-to-posterior KL exactly. Two things the implementation had to get right and would otherwise have got wrong without any symptom. The region: the model grid is deliberately wider than the data (D1's shift-plus-kernel margin), so its margin pixels are prior-only and are the largest eigenvalue of Sigma on essentially every real problem; the first working version reported the worst-determined mode at exactly the prior value, that is, it measured how much margin the grid was given. The eigenproblem is therefore restricted to the coordinates the design actually weights, taken from `diag(precision) - diag(Lambda_p)` (exactly zero where no epoch reaches, since the band assembly sums products of zeros), with the cut placed through the coverage ramp per component, because a global cut would delete a faint companion, whose whole row scales as `ell_2^2`. The comparison: `baseline=` derives the region from the full design and hands it to the subset, and both go through one `forecast()` closure, since letting each derive its own would compare two different questions, which is D46's defect class exactly. What it refuses to forecast is the orbit, for an instructive reason: the Fisher information for a velocity runs through `d(model)/dv ~ ell_i d_i'`, so an error bar on K_2 needs the line depths, which are what has not been measured yet. Measured, and the result corrects the roadmap's own premise: on a 13.7 d circular SB2 with eight epochs in hand and twelve to plan (`examples/08_forecast.py`), twelve more nights at P/2 keep the largest RMS differential velocity of the three plans (117.8 km/s) and are the worst of them, because two values of Delta leave `|g(k)|` recurring to J at a comb of scales, blind over 58% of the range, second mode 0.518, worth 243 nats. The same twelve spread over phase lower the RMS to 99.3 km/s, the blind fraction to 33% and the second mode to 0.071, and are worth 375 nats; simply continuing the existing cadence lands between them at 295. So math.md 5.1's `Var_j(Delta)` is the small-k expansion and not the objective, and maximizing it picks the worst of the three | `tests/test_forecast.py` (39); `tests/test_plotting.py`; `docs/api/forecast.md`; `docs/math.md` 5.5; `examples/08_forecast.py`; roadmap.md Tier 2 item 6 |
| D48 | `Disentangler(velocities=...)`: declaring measured velocities when there is no orbit | The façade's `Fit.free_velocities()` (D46) warm-starts from a Keplerian fit, and a cold start is measured at 122,000 nats worse (D42), so the free per-epoch RV table could only be reached through a fitted orbit. For a system with no published period that is circular, because the table is what produces the period, and it is the BLOeM case exactly, where the 59 published SB2s have no orbital solutions. `Disentangler` now takes `velocities=` (an `(n_stellar, n_epochs)` km/s table from cross-correlation, shift-and-add, or line splitting read by hand) as the alternative to `orbit=`, with exactly one of the two required, and `fit()` returns a velocity-mode `Fit` with no orbital sites sampled at all. The budget had to change source, and what it must not count is the interesting part. With no `k` priors to read, the bound comes from the declared table; but only the centred table reaches the model (`relative_velocities` removes one zero point per component), so a systemic velocity, +150 km/s for the SMC, is unidentified and must not be paid for in solver bandwidth. The term is therefore `sum_i max_j |v_ij - mean_j v_ij|`, times an explicit factor of 2 of headroom because the table is what the fit moves; both are itemized in `VelocityBudget.terms`, so `explain()` shows the derivation rather than a number. Centring for the bound is done in velocity space where the model does it in pixels; they differ by `O(v^2/c^2)` = 1e-8, against a factor-of-two headroom. The one failure mode of the mode is refused rather than left to be discovered: a declaration whose components never separate is the cold start, and it raises; velocities that separate by less than the widest LSF sigma warn, since a warm start inside the unresolved regime is close to a cold one. `scan()` and `detection_limit()` refuse without an orbit (both gate on `_scan_k`, so the refusal lives in one place). Measured: warm-started from velocities carrying 3 km/s of scatter and a 150 km/s systemic offset, the recovered table lands at 0.096 / 0.070 km/s against D42's Keplerian-warm-started 0.098 / 0.066, with per-epoch bars of 0.055-0.065 km/s and the systemic offset absent from both the answer and the budget | `tests/test_facade.py` (9 new); `docs/api/facade.md`; `docs/tutorials/bloem-sb2.md` §4; roadmap.md Tier 2 item 4 |
| D49 | Two exact speedups in the assembly, and four candidates rejected by measurement | Re-profiling came first, because D28's attribution had expired: D28 recorded "92% comb probing", and probing has not been on the hot path since. The split at the ladder's first row is band assembly 65% / block Cholesky 31% for an evaluation, and for a gradient, which is what NUTS runs ~2600 times, 82% assembly, whose backward cost 3.3x its own forward. Two changes, both exact. (1) `assembly._band_accumulate`, a `custom_vjp` for the band update. The forward is `band + place(f)`, the identity in `band`, but reverse mode transposes the `dynamic_update_slice` and the `dynamic_slice` separately and rebuilds that identity as `dus(out_bar, 0, idx) + dus(zeros, ds(out_bar, idx), idx)`: three passes over the whole band tensor per (i,j) block per epoch, 313 GB of traffic at the first ladder row, scaling with the band tensor rather than with the slice touched. The closed form (`band_bar = out_bar`, `f_bar = ds(out_bar, idx)`) costs nothing. (2) The second kernel application in `G = K^T H K` becomes one GEMM: unlike the first it translates columns only, so it is a contraction against a static `(w_u, w_g)` banded matrix rather than 2r+1 read-modify-write passes over the widest image in the assembly. Measured (row 0, 31,734 model px, p = 513, 50 epochs): evaluation 2.97 → 2.20 s, gradient 10.23 → 5.19 s, grad/eval 3.45x → 2.36x. Exactness: log-likelihood and gradient are bit-identical across stationary, AR(1) and wavelength-dependent-LSF fixtures; the Hessian moves 4e-13 relative (reassociation in the second-order path). D28's second-order re-entry defect recurred and was caught by D28's own regression test: a forward rule that calls its own custom function returns the transpose of the Hessian, fixed the same way, by inlining the primal. The price is paid in the right place: `custom_vjp` rejects `jax.jvp`, and `forecast._effective_parameters` was the package's only forward-mode site, since D47's `p_eff` came from one JVP of `log det` through `with_jitter`. Because `t` and the log-determinant are both scalars, forward and reverse return the same number, so it is now a `jax.grad`: bit-identical `p_eff` (3979.897960 either way, and `test_p_eff_matches_dense_trace` checks it against a dense trace oracle regardless), 0.28 s → 0.53 s once per forecast, against 1.8x on every leapfrog gradient. This is the same trade D28 made one stage later at `_solve_stage`, which is why `laplace_inverse_mass` was already reverse-over-reverse: the package now has no forward-mode path through the marginal, by construction rather than by accident. Decided on 2026-08-16 that it stays closed. Restoring forward mode means a real primitive with `jvp` and `transpose` rules across two custom boundaries, where the transpose must reproduce this pass's reverse rules bit for bit or the 1.8x is spent, and where the re-entry defect class has now been caught twice; the one formulation that supports both modes natively, the `.at[].add()` scatter, was measured in this pass at 4x slower forward with the gradient no longer bit-identical; and after the `jax.grad` switch the package has no forward-mode consumers left to pay for any of it. Revisit only when a concrete consumer lands (forward-over-reverse HVPs for a large-p Laplace, for example). The four candidates that did not survive measurement are as informative as the two that did. A blocked Cholesky looked like the obvious win, since XLA's fp64 `cholesky` at n=513 runs at 13 GFLOP/s against 249 for `matmul` at the same size on this machine, but a recursive factorization built from trsm+gemm bought only 1.36x, because neither trsm nor the leaf factorizations parallelize at that size either, so the block Cholesky is already near its practical ceiling. j-factoring the T-sandwich (`f_ij = A_i + frac_j B_i`, 16 slab operations down to 10) measured slower, because XLA already fuses the four tent terms and the restructure only adds a materialized intermediate. `remat=False` is slower rather than faster, so D28's memory choice was also the time choice. And the custom-VJP band→block packing named on D28's own ledger is real, since the gather does transpose to a scatter, but it costs 0.20 s of a 10 s gradient, so it stays unimplemented by decision rather than by omission | `tests/test_assembly.py` (2 new, plus the existing band-vs-probe equivalence set and the second-order regression that caught the re-entry defect); benchmarks.md "D49 speedup pass" |
| D50 | The three-way re-run: one machine, one protocol, and both misbehaving walls were environmental | Why a re-run rather than a patch: D49 declared the head-to-head hardware-bound with one wall (shift-and-add's 0.018 s) irreproducible, and ruled a partial update worse than none. Accuracy replicated before anything was timed: all twelve RMS values to the printed precision, exported fd3 inputs checksum-identical to the prior session, fd3's `.mod` byte-identical. The computations are fixed points, so only the measurement could have moved. The walls under one protocol (2–3 warmups, 9 recorded repeats, min and median, machine and stack recorded this time, since the original tables name neither, which is what left 0.018 s unfalsifiable): shift-and-add 0.0263 s, albireo 0.0591 s (jitted steady state, cold 0.495 s), fd3 0.0636 s pinned to one thread and 0.1042 s as found. The ranking on 16 cores is therefore shift-and-add first, albireo and single-threaded fd3 at parity, and M5's "albireo loses to both" is a fact about a laptop rather than about the codes. The irreproducible wall was the harness's, on both machines. The committed convention timed shift-and-add in-process after the XLA solve; the evidence: a dose-response (numpy-only 0.0265 s, after the jitted solve 0.0632–0.0736 s, unrecovered by `clear_caches` plus gc plus idle); not threads and not the CPU (pure NumPy, pinning flat, user ≈ wall, sys = 0, zero page faults: the same thread, ~2.7× slower, only when allocating); allocating ufuncs 4× slower while their `out=` twins are flat; and the Windows-CRT size structure (8 KB LFH flat, 34.6 KB, the exact temporary size, 0.23 → 2.14 µs, 800 KB 0.24 → 9.96 µs), XLA's allocation storm leaving >16 KB requests walking a user-mode free list. ~10⁴ allocations per call is +20–45 ms, which is the observed band; the committed convention reproduces 0.037–0.043 s and D49's 0.049 sits in the post-jit band. The laptop's 0.018 s carried its own unknowable dose, so the 1.46× clean-machine residual decomposes no further. `scripts/fd3_bench.py` now times shift-and-add in a fresh interpreter (warmup plus min of five). The control misbehaved too, usefully: fd3's C is single-threaded but its GSL links OpenBLAS, giving 1938% CPU in the default environment and 1.7× faster pinned, so D49's "fd3 barely moved across hardware" was partly oversubscription, and both figures are reported because as-found is how it gets run. Settled: shift-and-add is fastest everywhere, as a handful of array shifts should be; albireo ≈ pinned fd3 on many cores (min 7% ahead); accuracy (~2×) and the posterior are unchanged; the JOSS paper needed no edit, having never claimed a speed ordering; the M5 tables stand as history behind an annotation rather than a rewrite | `scripts/fd3_bench.py` (fresh-process shift-and-add timing); benchmarks.md "D50 re-run"; roadmap.md three-way caveat replaced |
| D51 | The incumbent's repository compared as software, and a notebook tour of the outputs | Three adoption-facing changes. (1) benchmarks.md gains "The incumbent's repository, feature for feature". The three-way table compares algorithms, so this is the comparison one level up: `TomerShenar/Disentangling_Shift_And_Add` as it ships. Orbit: a χ² grid over (K₁, K₂[, K₃]) with (P, T0, e, ω) supplied, against joint inference of every site. Uncertainty: χ² contours on K and nothing on the spectra, against posteriors on both. SB3: both (the model's hierarchical outer orbit at the expert level; the façade refuses it in v1). Masking: inside the published method, so it is not claimed as an advantage, and the modelled-component contrast (D40) is drawn instead. Distribution: unlicensed research scripts (GitHub API `license: null`, checked 2026-08-27; V2.0 2023-09-11, last commit 2024-03-07) against a BSD-3 package. The provenance rule is part of the record: the section was written from the README and API metadata only, with the source never opened, which is the same rule that kept `scripts/shift_and_add.py` clean-room; the page states the cost (the negativity constraint and the grid search were not measured here and cannot be reimplemented from source) and the obligation (whoever opens that repository must not afterwards edit the clean-room script). A head-to-head against the shipped code is recorded as legitimate and not done. (2) `docs/tutorials/showcase.ipynb`, an executed notebook touring every headline output, wired into the site by `mkdocs-jupyter`, which had been a declared docs dependency since the D39 block and was never configured. `execute: false` on the plugin: the committed outputs are what the site renders, so the docs build stays offline and free of a JAX-plus-sampling step, and re-execution is a release-time act (`scripts/build_showcase_notebook.py`, ~10 min of NUTS) whose post-pass strips kernel-environment stderr noise and palette-quantizes the inline PNGs, 884 → 373 KiB, under the 500 kB pre-commit file-size limit. The notebook runs the packaged example through the façade: explain, fit summary, banded spectra against the truth resampled onto the model grid (the façade's derived grid is wider than the generation grid, and the first execution failed on exactly that mismatch), residual z-scores, `sample()` with its plug-in caveat printed, RV-curve draws with the injected epoch velocities overlaid, a corner plot, joint-posterior draws, and `sensitivity_forecast` of six planned nights against the twelve in hand via `plan_epochs` and `baseline=`. Ruff lints and formats notebooks, so the cells are held to the same rules as the package. (3) `example_info("sb2_sim")` said "circular"; the example is e = 0.15 by design (D39: a circular example would start every new user at the (√e cos ω, √e sin ω) singularity), and the generator's own comment says so, so a claimed property of shipped data contradicted its own generator, which is D45's failure shape one level down. Fixed to state e = 0.15 | benchmarks.md "The incumbent's repository, feature for feature"; docs/tutorials/showcase.ipynb; scripts/build_showcase_notebook.py; mkdocs.yml; src/albireo/examples.py; CHANGELOG.md |
| D52 | Synthetic spectral libraries and differentiable interpolation (`albireo.library`) | A standardized container for published grids (node labels, normalized flux, continuum, wavelength scale) plus two differentiable interpolators. The medium is a required field with no default, because air and vacuum differ by ~83 km/s and the upstream documentation is not a reliable source: BOSZ 2017 was vacuum throughout and BOSZ 2024 is air above 200 nm, under the same name, so a cached copy from the wrong year is an 80 km/s error that no code path would catch. `line_core_medium` therefore measures the convention, using parabola-refined line cores against both conventions, which differ by ~1.5 Å where a correctly located core lands within hundredths, and the ingest paths use it to verify a declaration rather than to supply one; the helper validates itself on synthetic spectra built both ways, where the same code must return opposite answers. Interpolation is in flux, never in atmospheres (Mészáros & Allende Prieto 2013 measured 0.19% against 0.051% linear and 0.031% cubic on exactly BOSZ's 250 K / 0.5 dex spacing), and the continuum in the log, which is both accurate across decades of Teff and what guarantees the positivity the light-fraction simplex needs. Two geometries: separable Catmull-Rom on a complete axis product, and barycentric interpolation over a Delaunay triangulation where physics has cut the grid's corners off, as it has for every public OB library. Both reproduce a node exactly (`==`, not a tolerance), which is what lets the warm-start scan and the continuous fit be compared on one footing. Two things measurement changed. The cubic's phantom end nodes are extrapolated linearly rather than clamped: clamping is the usual quick fix and it destroys linear reproduction in the edge cells, which on a 7-value axis made the cubic lose to plain multilinear over a third of the range, caught because the oracle test disagreed only at edge cells. And the Delaunay path drops zero-volume simplices: a published grid is a lattice with pieces removed, which is exactly the cospherical configuration Qhull cannot triangulate uniquely, so 83 of 515 simplices came back with a NaN barycentric transform, enough to poison `argmax` and return a NaN spectrum. Joggling would have fixed the degeneracy at the cost of moving the nodes, that is, of the exactness invariant, so the degenerate simplices are filtered instead. `crossval_library` is the measurement that decides whether a given grid needs a learned emulator at all, applying the D49/D50 discipline to a question the literature usually settles by assertion. The registry, added after the interpolators, makes the grids obtainable: `fetch_library` builds a named library on first use and caches it, keyed by a version token in the filename so a re-pinned registry cannot read a stale build. BOSZ is fetched automatically because its MAST URLs are deterministic, and three facts about them were checked against the live archive rather than read off the documentation, two of which contradict a careful reading: Teff is not zero-padded (`t6000`), and the atmosphere code changes across the grid (MARCS spherical below log g 3.5, plane-parallel above, ATLAS9 above 8000 K). Both cost a 404 in the first draft and are now pinned by tests. POLLUX has no stable URL, its collections coming through a form POSTing to `/download/`, so `ingest_pollux` documents the manual step and refuses rather than shipping a parser for a format nobody has inspected. The medium is measured on every build and a disagreement with the registry raises: on the real grid it returns air at a ratio of 256:1, residuals 0.001–0.014 Å against air against 1.17–1.80 Å against vacuum. Two bugs the end-to-end build exposed: the cached digest was self-referential, since the file's hash was written into the file, so it could never match on reload, and it is now taken over the arrays, which also makes it comparable across machines; and a single-valued axis (a library sliced to one metallicity) produced an empty interpolation cell, lo == hi, and a 0/0 that reached every pixel as a silent NaN, so a degenerate axis is now constant along itself. The build path also reads back what it wrote, so a warm cache and a cold one return bit-identical arrays rather than differing by whether the fluxes had been through float32 yet | `tests/test_library.py` (51, oracles: scipy `RegularGridInterpolator`, `LinearNDInterpolator`, an in-test NumPy Catmull-Rom, `grids.air_to_vacuum`, and a faked transport for the downloads, plus one `network`-marked liveness check); `docs/math.md` §9.3; `docs/api/library.md` |
| D53 | Label matching against public grids (`albireo.match`), and the roadmap non-goal it does not reopen | Fits Teff, log g, [M/H] and v sin i to disentangled components so that they can be rendered as RV templates. The scope is stated in its own docstring before the method is described: no synthesis, no line list, no radiative transfer, no abundances. `albireo.handoff` remains the route to GSSP, iSpec, Korg.jl and PySME, and roadmap.md's non-goal is amended to say so explicitly rather than widened silently. Three design decisions, each of which a naive version gets wrong. (1) Dilution is the problem, not a detail. §9.1: the disentangler solved against an assumed constant ℓ⁰, so what it recovered is (w/ℓ⁰)·t, and an error in ℓ⁰ rescales every line depth and is indistinguishable from a temperature error. Following GSSP's `gssp_binary`, both components are fitted jointly through one shared radius ratio, with wavelength-dependent light fractions written as a softmax over the grids' own log-continua, so Σw = 1 holds at every pixel by construction, with no constraint site and nothing to drift. `FixedDilution` is kept as the diagnostic that shows what this bought; the closed-loop test injects deliberately wrong light fractions and asserts that the ratio absorbs them while a rigid fit's temperatures move instead. (2) The nuisance is additive, and that is not a style choice. The k = 0 null space of §5.1 lives in the continuum, where the deviation spectrum is zero, so a multiplicative continuum polynomial, the usual choice in single-star work, is identically zero there and cannot represent it. The zeroth Chebyshev term is the unconstrained zero point, fitted and reported rather than absorbed; a dedicated test adds a constant to one component and requires the labels not to move, and requires them to move when the nuisance is removed, so the term is verified rather than assumed. (3) The comparison happens at native resolution, a default corrected by D55, having shipped as `matched`. d̂ is a regularized partial deconvolution (§1.3 applies the LSF inside the epoch model), which argued for convolving both sides once with the declared kernel. That argument is right about the deconvolution and wrong about its cost, and real data said so: see D55. Uncertainty is quoted twice and labelled: Laplace by curvature, and the spread from refitting joint posterior draws of the component spectra, which internalizes the `handoff.export_draws` loop. Three defects the closed loop caught, all invisible to a passing chi-square. The Laplace diagonal was zipped against sorted site names while the Chebyshev sites are length-3 vectors, so every uncertainty after the first vector site read the wrong row, surfacing as an implausible ±0.012 K rather than as an error; row labels are now built from the sites' shapes. The jitter site was unbounded, and since its own ML point is the RMS residual it runs to zero scale on an over-good fit, taking the gradient norm to 10⁶ and stalling L-BFGS; it is bounded to a factor of five, which is an assumption worth stating anyway. And the warm-start scan could hand numpyro a library node the prior excluded, whose only symptom was "cannot find valid initial parameters"; the scan is now restricted to nodes the declared priors permit, with an error that says which. Closed loop on a toy grid: injected off-node labels recovered to <0.1 K in Teff, 0.0003 dex in log g and 0.004 km/s in v sin i with no noise, and to ~10–75 K at S/N tiers 300 and 100, against a documented target of 2–3% (§9.6) | `tests/test_match.py` (26, incl. the zero-point-immunity and wrong-ℓ⁰ invariants); `docs/math.md` §9.1–9.6; `docs/api/match.md`; `docs/roadmap.md` non-goals |
| D55 | AI Phoenicis: the label fit against a star, and the comparison mode it corrected | The real-data gate for D52/D53. 36 archival HARPS spectra of AI Phe (R = 115,000), disentangled on the Mg I b window with the velocities held at the published orbit so that the label fit is what is under test. AI Phe is the target because every quantity the mode produces has an independent published value (Teff 6310/5010 K, log g 4.001/3.598, R₂/R₁ = 1.6237, Maxted et al. 2020), and the log g values are derived from the spectroscopic and photometric elements (`g_i = 2π√(1−e²)K_j / (P r_i² sin i)`, no absolute masses needed, checked against the published ones to 0.002 dex) rather than quoted. Result: the primary lands at +0.52%, inside the 2–3% target; the secondary at +4.33% does not, and is recorded as a miss. The radius ratio, which the fit is never told, comes back 5% low from spectroscopy alone. Freeing log g reproduces the documented failure on real data: it runs to the grid edge, drags both temperatures down, and improves χ², with the corollary that the correlation report comes back empty, because a parameter pinned against a bound stops varying and the curvature stops showing the degeneracy that produced the answer. The finding that changed code: `compare="matched"` was wrong as a default. Convolving both sides correlates the residuals over the kernel width while the likelihood stays diagonal, so χ² is over-counted by `1/Σk²`, predicted 4.91 at σ_LSF = 1.38 px and measured 4.26, the entire gap, and *v* sin *i* absorbs the mis-specification, both components pinning to the floor of their prior (0.14/0.46 km/s) where native returns a physical 2.2. The default was flipped to `native`. The closed loop could not have caught it, and that is the transferable lesson: its injected rows never pass through an LSF or a disentangling, so they are intrinsic spectra and both modes recover them; a toy fixture validates the arithmetic it contains, never the assumption it was built on. A hypothesis was tested and refuted, recorded so that it is not re-bought: the library's pinned ξ = 2 km/s was the obvious suspect for the secondary (ξ = 2 gives 8.45% more equivalent width than ξ = 1 at that node, the right direction), but a 160-node rebuild at ξ = 1 made the secondary worse (+292 K against +273 K) and χ² with it; the change went into [M/H], +0.10 dex, which is the [M/H]–ξ degeneracy of §9.2. Most of the secondary's offset remains unexplained; candidates are named in benchmarks.md, untested | `scripts/aiphe_labels_bench.py`, `scripts/download_aiphe.py`; `docs/tutorials/aiphe-labels.ipynb` (executed); `docs/benchmarks.md` "D55"; `tests/test_match.py::test_the_default_comparison_is_native`; `docs/math.md` §9.2 |
| D56 | Epoch radial velocities by N-dimensional correlation (`albireo.todcor`) | The one product the joint fit never makes, one velocity per component per epoch, and the one every eclipsing-binary analysis, survey pipeline and orbit code consumes. Built as the weighted least-squares fit of N shifted templates to the observed pixels, with the amplitudes held or solved in closed form at every set of shifts, which on a uniform grid with uniform weights is Zucker & Mazeh's (1994) TODCOR: both their symmetric expression and the fixed-ratio one are pinned to 1e-10 against a NumPy transcription, and the formulation generalizes to any N (Zucker et al. 1995; Torres et al. 2007 are the N = 3, 4 cases of the same block solve). Three decisions. (1) Project the templates onto the data, never the reverse: the shifted, LSF-convolved template is rebinned onto each epoch's native pixels by the same operators as the forward model, so masks, gaps, cosmics, per-pixel weights, mixed instruments and mixed samplings enter through the weights and change no formula (D4). One segment-sum builds every shifted column, one matrix product per template pair every Gram block; the global search strides the grid by the narrowest LSF sigma, which cannot step over a peak. (2) The sub-pixel minimum is computed rather than interpolated: the shift operator is linear in the template, so every inner product is bilinear in the fractional shifts and the chi-square is an exact quadratic inside each unit cell, reconstructed from 3^N exact evaluations and minimized in closed form, with the amplitude solve alternating when profiled. The same fact bounds the operator's pixel-locking ripple at ~0.1/sigma_px^2 px (math.md §10.3), which is why `Fit.templates()` upsamples to three pixels per sigma and `todcor` warns below two. (3) Errors are Zucker's (2003) maximum-likelihood ones by default: the curvature of the surface rescaled by the reduced chi-square (`errors="profiled"`), with the trusted-weights version alongside, and the covariance's off-diagonal is the blending flag. Light fractions default to `"global"`: fitted freely per epoch, then the weighted median over well-detected unblended epochs of each instrument is held. This is the standard practice, because a per-epoch ratio is noisy and one fitted at a blended phase is not a measurement; `Fit.measure_velocities()` holds the declared fractions instead, being the only amplitudes consistent with components solved against them (§9.1). The zero point is stated on every table. A synthetic template is absolute; a disentangled component's rest frame is the unidentified constant of §5.3, so velocities against it are differential and `absolute=False` travels with them into the orbit fit. Two departures from published practice, both deliberate: continuum-normalized data pin the composite's scale, so the fractions are held exactly by default rather than leaving the scale free (`scale="free"` restores the classic form); and orders are pooled through their declared weights rather than through the per-order likelihood product. Closed loop on a simulated SB2 through LSF, rebin, cosmics, gaps and barycentric motion: every velocity inside 5 sigma, rms ~0.03-0.07 km/s at S/N 150 on a 1.5 km/s grid, pull rms 1.0 | `tests/test_todcor.py` (33, incl. the three identities); `docs/math.md` §10; `docs/api/todcor.md`; `docs/tutorials/todcor.md`; `examples/12_todcor.py`; `scripts/todcor_bench.py`; benchmarks.md "D56" |
| D58 | One command for a list of stars (`albireo.pipeline`, the `albireo` CLI) | Every stage existed as a function and nothing ran them together for a list of stars with the failures recorded and the results in a table, which is what a survey runs and what a first-time user asks for. Built as a driver rather than a method: each stage is called as the user would call it, and the two rules the stages enforce are enforced unchanged (light fractions declared, never defaulted; the wavelength medium declared before a synthetic grid is consulted). Where a request cannot be honoured, such as a label fit on files that declare no medium, or an orbit from four usable epochs, the star gets a flag and the batch carries on. The configuration is the façade's own vocabulary rendered as TOML (stdlib `tomllib`, no new dependencies), which is the ordering the roadmap asked for: the CLI came after the façade so as not to freeze a second schema. Three routes into the orbit: a period prior; `period = "search"`, bootstrapping from library templates through a periodogram and `RVOrbit.to_theta()`; and a file of measured velocities into the free table. The zero point travels: when the label fit has measured a component's frame offset the pipeline applies it to that component's own template, so the epoch velocities come out absolute and the orbit fit recovers the systemic velocity the disentangling alone cannot see (the demo's toy star: +11.9 ± 0.2 against an injected +12); without it the table is differential and says so. Batches run in a spawn-based process pool: 2.0× for four workers and 2.5× for eight on a batch of eight simulated stars, sub-linear because one star already keeps several cores busy. Each worker's XLA and BLAS threads are capped at `cpu_count // jobs` as a precaution, and the benchmark found that the cap made no measurable difference (54.1 against 54.7 s). A worker returns plain data, and the live objects are kept only in-process. The degeneracy the driver found: with a symmetric semi-amplitude prior the conjunction scan sees the declared component assignment and its mirror (spectra swapped, rescaled by the light ratio) as equally deep, and the first demo run landed in the mirror. It is resolved by convention, stated rather than hidden: components are declared in order of decreasing mass and the fit is started with K₁ < K₂, and the label stage flags a fitted light fraction far from the declared one as the signature of a wrong order. Finding it exposed a façade defect, since `_vector_spec` dropped every entry's `start_at`, so per-component semi-amplitude declarations always started at their midpoints; now fixed with a regression test | `tests/test_pipeline.py` (19, incl. two slow closed loops and a two-process batch); `tests/test_cli.py` (6, incl. FITS on disk through the command); `tests/test_facade.py::test_a_vector_site_keeps_each_entry_starting_value`; `docs/api/pipeline.md`; `docs/tutorials/pipeline.md`; `examples/13_pipeline.py`; `scripts/pipeline_bench.py`; benchmarks.md "D58" |
| D57 | Orbits from velocity tables (`albireo.rvorbit`), and the loop closed through the façade | The next thing anyone does with a velocity table is fit a Keplerian to it, and doing it here, as weighted least squares with the JAX Jacobian, the same Kepler solver and the same angle conventions as `orbit_velocities`, means the two routes compare element for element. One systemic velocity per component is used whenever a component is differential: a table from disentangled templates carries one unidentified zero point per star, and a shared gamma across two different constants is absorbed into both semi-amplitudes (the test forces it and watches K corrupt). Circular fits are parameterized in (e cos w, e sin w) at the origin rather than at the singular point of the sqrt(e) form (D39's trap). Errors are rescaled by the reduced chi-square, stated as what every orbit code does rather than as a claim that the per-epoch errors were right. `find_period` runs the Lomb-Scargle periodogram on the difference of the first two components, which is free of both systemic velocities and both zero points, and returns the aliases, because a sparse table's periodogram is rarely unambiguous. `RVOrbit.to_theta()` returns the elements as `Disentangler(orbit=...)` takes them, so a library-template measurement can warm-start a disentangling. The façade closes the loop in the other direction: `Fit.templates()` turns the stellar components into templates (upsampled, intrinsic, zero point declared unknown) and `Fit.measure_velocities()` runs the epochs against them with the declared fractions and the declared LSF per instrument. On the packaged example the self-consistent table recovers the injected velocities to <0.5 km/s once each component's zero point is removed, and the orbit fitted to it returns K to <3% | `tests/test_rvorbit.py` (7); `tests/test_todcor.py::test_fit_templates_close_the_loop_on_the_packaged_example` (slow); `docs/math.md` §10.6; `docs/api/rvorbit.md`; `examples/12_todcor.py` |

---

## 3. Data model

The core takes arrays; instrument I/O is out of scope (one thin loader utility only).

```
EpochData:                      # one observed spectrum
    wave        (N_j,) float64  # native wavelength grid, Å (vacuum or air — declared, not guessed)
    flux        (N_j,) float64  # continuum-normalized
    ivar        (N_j,) float64  # inverse variance; 0 = masked
    mask        (N_j,) bool     # optional convenience; folded into ivar
    bjd         float           # BJD_TDB mid-exposure
    v_bary      float           # barycentric correction velocity [km/s]
    instrument  str             # key into the LSF/response tables

Dataset: list[EpochData] + frame flag ("topocentric" | "barycentric"), validation, summary stats
```

Internally the dataset is packed into padded/ragged JAX arrays batched by instrument. Chip
gaps, cosmics and interstellar lines are all `ivar = 0`, with no special cases anywhere
downstream (weights and masks are honored in every operator and every solve).

## 4. Model & inference architecture

One equation summary (derivations in math.md §1–3): per epoch,
`m_j = diag(r_j) · R_j · [1 + Σ_i ℓ_ij · B_j · T(δ_ij) · d_i]`, i.e. shift → LSF-convolve →
rebin-to-native → response, linear in the stacked deviation spectra `d`. Marginalize `d`
analytically → sample θ with NUTS → recover `p(d | y)` as a mixture of conditional Gaussians
over posterior θ draws.

**Parameter split.**
- Linear (marginalized): component deviation spectra `d_i`, ~10⁵–10⁶ numbers that are never
  sampled.
- Nonlinear θ (sampled): orbit (~5–8), light fractions (0–N_c·J), LSF widths (per instrument),
  response coefficients (J·(order+1)), jitters (≤J), prior hypers (≤2·N_c). Typically 20–250.

**Modes.** All share one code path; a mode is a choice of velocity law plus what is held fixed.
1. SB2/SB3 Keplerian joint inference (the primary mode).
2. Free per-epoch velocities (diagnostic).
3. Fixed-orbit linear solve (M2 milestone; also the "spectra now" utility).
4. SB1 + faint companion K₂ scan → Δlogℒ detection maps + recovered secondary spectrum
   (math.md §6), vmapped over the grid.

**Outputs** (`FitResult` / `PosteriorResult`): θ posterior (arviz-compatible), conditional
spectra draws + pointwise bands + banded covariance info, whitened residual diagnostics,
degeneracy report (low-k variance spectrum, response–spectrum covariances), sensitivity
forecast utility.

## 5. Degeneracy policy

Derivations in math.md §5. Policy: a real degeneracy is never regularized away without notice.
It is made proper with an explicit prior scale and reported, and where only external
information resolves it, the user is required to choose.

| Degeneracy | Policy |
|---|---|
| Low-frequency mode exchange ("undulations") | proper priors with documented scale; posterior covariance reports inflation; `sensitivity_forecast()` for observing strategy |
| Light ratio ↔ line depth | explicit choice required: `Fixed` / `Free(prior=...)` / `PerEpoch(eclipse model)`; the docs explain what breaks it |
| γ ↔ common shift | γ ≡ 0 default; post-hoc template measurement; free-γ requires an informative prior |
| Response ↔ broad features | low default order; covariance reported |

## 6. API sketch (target user code)

```python
import albireo as ab

ds = ab.Dataset(
    [ab.EpochData(wave=w, flux=f, ivar=iv, bjd=t, v_bary=vb, instrument="HERMES"), ...],
    frame="topocentric",
)

dis = ab.Disentangler(
    dataset=ds,
    grid=ab.LogGrid.from_wavelength_range(4000.0, 6800.0, dv_kms=1.0),
    components=[ab.Star("A"), ab.Star("B"), ab.Telluric()],
    orbit=ab.Keplerian(
        period=ab.Normal(11.55, 0.01),  # priors are first-class
        t_peri=...,
        ecc=...,
        omega=...,
        k1=...,
        k2=...,
    ),
    light_ratio=ab.FixedLight([0.62, 0.38]),  # explicit, or Free(prior=...) / PerEpoch(...)
    lsf={"HERMES": ab.GaussianLSF(sigma_v_kms=2.4)},
    response=ab.Chebyshev(order=2),
    spectral_prior=ab.Smoothness(tau="ml2", eta=1e-4),
)

quick = dis.fit_map()  # MAP + conditional spectra, seconds
post = dis.sample(num_warmup=1000, num_samples=1000, seed=0)  # NUTS over θ
spec = dis.conditional_spectra(post)  # draws, bands, covariance
rep = dis.degeneracy_report(post)

# diagnostic + detection modes
free = dis.replace(orbit=ab.FreeVelocities()).fit_map()
scan = ab.K2Scan(dis, k2_grid=np.linspace(1, 150, 300)).run()  # SB1 workflow
```

## 7. Package layout & engineering

```
src/albireo/
    __init__.py        # x64 enforcement, public API re-exports
    grids.py           # LogGrid, doppler log-shift mapping          [M0 — this session]
    operators.py       # shift/interp/rebin linear ops + adjoints    [M0 — this session]
    kepler.py          # differentiable Kepler solver, velocity laws [M1]
    simulate.py        # simulator = test harness                    [M1]
    forward.py         # epoch forward model assembly                [M2]
    solver.py          # banded/block-tridiag Cholesky, logdet, solves, sampling [M2]
    likelihood.py      # marginal likelihood (strategies A/B/C)      [M2–M3]
    priors.py          # spectral priors, parameter priors           [M2]
    inference.py       # MAP (optax), NUTS (numpyro), results        [M3]
    scan.py            # K2-scan mode                                [M4]
    data.py            # EpochData/Dataset, validation, thin loader  [M1]
    preprocess.py      # continuum, ivar estimation, region/masking, grid sharing [D30]
    io.py              # FITS -> RawSpectrum -> EpochData/Dataset (astropy) [D30]
tests/                 # mirrors modules; simulator-driven closed-loop tests
docs/                  # mkdocs-material; design.md, math.md, tutorials (executable, in CI)
```

Standards (per brief): JAX with x64; CPU-first correctness, GPU as accelerator; type hints +
docstrings on all public API; pytest with closed-loop tolerance tests, adjoint tests for every
linear operator, gradcheck vs. finite differences; GitHub Actions CI on ubuntu + windows;
pre-commit (ruff, ruff-format); BSD-3; SemVer. Performance work is benchmark-gated: no
optimization without a recorded baseline in `docs/benchmarks.md` (started at M2).

Test philosophy: the simulator (M1) is the oracle. Every inference feature ships with a
closed-loop test against known injected truth, and calibration claims (M3+) are backed by
SBC/coverage runs rather than asserted.

## 8. Milestones

| | Deliverable | Acceptance gate |
|---|---|---|
| **M0** | scaffolding, design.md, math.md, grid + shift/resample operators with adjoint/gradcheck/flux tests | this session; stop for review |
| M1 | simulator (templates, orbit, LSF, noise, tellurics, response) | simulated datasets reproduce all advertised pathologies (gaps, mixed instruments) |
| M2 | fixed-orbit linear solver + uncertainties | <1% RMS recovery in line regions on SNR-100/30-epoch sims; whitened residuals ~N(0,1); marginal likelihood matches dense brute force on small problems; nullspace analysis documented |
| M3 | joint NUTS inference + MAP pipeline | SBC/coverage pass on injections; K₁,K₂ to <1% at design SNR |
| M4 | tellurics, SB3, per-epoch light, multi-instrument LSF, K₂-scan | closed-loop tests per feature |
| M5 | benchmarks vs. fd3, one real published SB2 end-to-end, docs polish, JOSS checklist | definition of done: 2×10⁵ px / 50 epochs samples in minutes on one GPU; tutorials run in CI. Candidate headline case: an LB-1/HR 6819-type system, where the posterior exposes the hand-set-light-ratio systematic that drove the published debate |

## 9. Non-goals (v1) and v2 seams

Not in v1: GUI; Fourier-space methods; time-variable component spectra (pulsations/spots);
emission-line-specific models; apsidal motion; exhaustive instrument I/O.

Designed seams for v2: components carry their own velocity-law and (future) time-dependence
interface, so a pulsating component would subclass the component protocol without touching the
solver; the LSF is an operator slot (Gaussian → tabulated is a swap); the Keplerian core is a
standalone module shared with a future differentiable eclipsing-binary package for joint
light-curve + RV + disentangling fits; strategies A/B share one solver interface so that new
structured solvers slot in.
structured solvers slot in.
